"use client";

import { useEffect, useState } from "react";
import type { LucideIcon } from "lucide-react";
import { AlertCircle, AlertTriangle, Archive, ArrowLeft, ArrowLeftRight, ArrowRight, Banknote, Bell, Boxes, CalendarDays, CalendarRange, Camera, ChartNoAxesCombined, Check, ChevronLeft, ChevronRight, CircleCheck, CircleDollarSign, ClipboardList, Clock3, Copy, Download, ExternalLink, FileBarChart, FileDown, History, LayoutDashboard, ListPlus, LogOut, Mail, MapPin, Menu, Minus, Moon, Pencil, Phone, Plus, Receipt, RefreshCw, RotateCcw, Search, Settings, ShieldCheck, Sparkles, SlidersHorizontal, Sun, TrendingDown, TrendingUp, Users, WalletCards, Wrench, X } from "lucide-react";
import { getLocalDatabase } from "../lib/local-database";
import { formatManilaTime } from "../lib/manila-datetime";
import { deleteRecord } from "../lib/delete-record";
import { DialogHost, showConfirm, showNotice } from "../lib/ui-dialogs";
import { PackageDetailActions } from "./package-detail-actions";
import { PackageEditForm } from "./package-edit-form";
import { DEFAULT_PACKAGE_CALENDAR_COLOR, PACKAGE_CALENDAR_COLORS } from "../lib/package-calendar-colors";
import { BookingEditForm } from "./booking-edit-form";
import { BookingReconciliationForm } from "./booking-reconciliation-form";
import { BookingPreparationForm } from "./booking-preparation-form";
import { MaintenanceActionForm } from "./maintenance-action-form";
import { GoogleCalendarControls } from "./google-calendar-controls";
import { GoogleCalendarSettings as GoogleCalendarSetupSettings } from "./google-calendar-settings";
import { InvoicesView } from "./invoices-view";

const navigation = [
  ["Overview", LayoutDashboard], ["Bookings", ClipboardList], ["Clients", Users],
  ["Calendar", CalendarRange], ["Cashflow", WalletCards], ["Expenses", Minus],
  ["Inventory", Boxes], ["Equipment", Camera], ["Packages", ListPlus], ["Reports", ChartNoAxesCombined],
] satisfies ReadonlyArray<readonly [string, LucideIcon]>;
type AuthUser = { id:string; fullName:string; email:string; phone:string; facebookUrl:string; role:"ADMIN"|"USER"; profilePhoto:string|null };

function Skeleton({ className = "" }: { className?: string }) {
  return <span className={`skeleton ${className}`} aria-hidden="true" />;
}

function LoadingScreen({ label = "Loading workspace" }: { label?: string }) {
  return <div className="loading-screen" role="status" aria-live="polite"><div className="loading-mark" aria-hidden="true"><span>S</span></div><div className="loading-dots" aria-hidden="true"><i /><i /><i /></div><p>{label}<span>Preparing your studio workspace</span></p></div>;
}

export default function Home() {
  const [loading, setLoading] = useState(true);
  const [activeView, setActiveView] = useState("Overview");
  const [viewRestored, setViewRestored] = useState(false);
  const [darkMode, setDarkMode] = useState(false);
  const [notificationsOpen, setNotificationsOpen] = useState(false);
  const [authUser, setAuthUser] = useState<AuthUser | null>(null);
  const [authChecked, setAuthChecked] = useState(false);
  const [showBookingForm, setShowBookingForm] = useState(false);
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [dashboardStats, setDashboardStats] = useState({ revenue: 0, bookings: 0, outstanding: 0, lowStock: 0, paidExpenses: 0, verifiedCash: 0 });
  const [upcomingBookings, setUpcomingBookings] = useState<Array<{ id: string; event_name: string; event_date: string; start_time: string | null; end_time: string | null; venue: string | null; maps_url: string | null; total_amount: number; downpayment_amount: number; clients: { display_name: string } | null; service_packages: { name: string } | null }>>([]);
  const [actionFlags, setActionFlags] = useState<{ overdueExpenses: number; lowStock: number; pendingBookings: number }>({ overdueExpenses: 0, lowStock: 0, pendingBookings: 0 });
  const [dashboardError, setDashboardError] = useState("");
  const [readiness, setReadiness] = useState({ businessData: false, packages: false, consumables: false, calendar: false });
  const todayLabel = new Intl.DateTimeFormat("en-PH", { timeZone: "Asia/Manila", weekday: "long", month: "long", day: "numeric", year: "numeric" }).format(new Date());
  const hourInManila = Number(new Intl.DateTimeFormat("en-US", { timeZone: "Asia/Manila", hour: "numeric", hour12: false }).format(new Date()));
  const greeting = hourInManila < 12 ? "Good morning" : hourInManila < 18 ? "Good afternoon" : "Good evening";

  useEffect(() => {
    fetch("/api/auth/me", { cache: "no-store" }).then((response) => response.json()).then((result) => { if (result.user) setAuthUser(result.user as AuthUser); else window.location.replace("/login"); }).catch(() => window.location.replace("/login")).finally(() => setAuthChecked(true));
  }, []);

  useEffect(() => {
    const storedView = window.localStorage.getItem("salikha-active-view");
    const validViews = navigation.map(([label]) => label).concat("Invoices", "Settings", "Team");
    if (storedView && validViews.includes(storedView)) setActiveView(storedView);
    setViewRestored(true);
    const timer = window.setTimeout(() => setLoading(false), 650);
    return () => window.clearTimeout(timer);
  }, []);

  useEffect(() => {
    const theme = window.localStorage.getItem("salikha-theme");
    const dark = theme === "dark";
    setDarkMode(dark);
    document.documentElement.dataset.theme = dark ? "dark" : "light";
    const onKeyDown = (event: KeyboardEvent) => {
      if (event.key !== "Escape") return;
      if (notificationsOpen) { setNotificationsOpen(false); return; }
      const overlays = [...document.querySelectorAll<HTMLElement>(".booking-overlay")];
      const topmost = overlays.at(-1);
      if (!topmost) return;
      const close = topmost.querySelector<HTMLButtonElement>(".close-button") || topmost.querySelector<HTMLButtonElement>(".drawer-footer .secondary-button");
      close?.click();
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [notificationsOpen]);

  const toggleTheme = () => { const next = !darkMode; setDarkMode(next); document.documentElement.dataset.theme = next ? "dark" : "light"; window.localStorage.setItem("salikha-theme", next ? "dark" : "light"); };

  useEffect(() => {
    if (viewRestored) window.localStorage.setItem("salikha-active-view", activeView);
  }, [activeView, viewRestored]);

  useEffect(() => {
    let cancelled = false;
    const database = getLocalDatabase();
    if (!database) return;
    const todayIso = manilaDateKey(new Date());
    (async () => {
      const [bookingsRes, expensesRes, inventoryRes, cashAccountsRes, cashTxRes, clientsRes, packagesRes] = await Promise.all([
        database.from("bookings").select("id, event_name, event_date, start_time, end_time, venue, maps_url, status, total_amount, downpayment_amount, paid_amount, clients(display_name), service_packages(name)"),
        database.from("expenses").select("id, status"),
        database.from("inventory_items").select("id, on_hand, reorder_level, is_active"),
        database.from("cash_accounts").select("id, opening_balance"),
        database.from("cash_transactions").select("id, transaction_date, status, direction, amount"),
        database.from("clients").select("id", { count: "exact", head: true }),
        database.from("service_packages").select("id, is_active"),
      ]);
      if (cancelled) return;
      const failedQueries = [["Bookings", bookingsRes.error], ["expenses", expensesRes.error], ["inventory", inventoryRes.error], ["cash accounts", cashAccountsRes.error], ["cash transactions", cashTxRes.error], ["clients", clientsRes.error], ["packages", packagesRes.error]].filter((entry) => entry[1]).map((entry) => `${entry[0]}: ${(entry[1] as { message: string }).message}`);
      setDashboardError(failedQueries.join(" · "));
      const bookings = ((bookingsRes.data || []) as unknown) as Array<{ id: string; event_name: string; event_date: string; start_time: string | null; end_time: string | null; venue: string | null; maps_url: string | null; status: string; total_amount: number; downpayment_amount: number; paid_amount: number; clients: { display_name: string } | null; service_packages: { name: string } | null }>;
      const expenses = (expensesRes.data || []) as Array<{ id: string; status: string }>;
      const inventory = (inventoryRes.data || []) as Array<{ id: string; on_hand: number; reorder_level: number; is_active: boolean }>;
      const accounts = (cashAccountsRes.data || []) as Array<{ id: string; opening_balance: number }>;
      const txs = (cashTxRes.data || []) as Array<{ id: string; transaction_date: string; status: string; direction: string; amount: number }>;
      const monthKey = todayIso.slice(0, 7);
      const postedThisMonth = txs.filter((transaction) => transaction.status === "POSTED" && bookingDateInput(transaction.transaction_date).slice(0, 7) === monthKey);
      const revenue = postedThisMonth.filter((transaction) => transaction.direction === "INFLOW").reduce((sum, transaction) => sum + Number(transaction.amount || 0), 0);
      const paidExpenses = postedThisMonth.filter((transaction) => transaction.direction === "OUTFLOW").reduce((sum, transaction) => sum + Number(transaction.amount || 0), 0);
      const activeBookings = bookings.filter((booking) => booking.status === "PENDING");
      const outstanding = activeBookings.reduce((sum, booking) => sum + Math.max(0, Number(booking.total_amount || 0) - Number(booking.downpayment_amount || 0)), 0);
      const verifiedCash = accounts.reduce((sum, a) => sum + Number(a.opening_balance || 0), 0) + txs.filter((t) => t.status === "POSTED").reduce((sum, t) => sum + (t.direction === "INFLOW" ? Number(t.amount) : -Number(t.amount)), 0);
      const lowStock = inventory.filter((item) => item.is_active && Number(item.on_hand) <= Number(item.reorder_level)).length;
      setDashboardStats({ revenue, bookings: activeBookings.length, outstanding, lowStock, paidExpenses, verifiedCash });
      const currentMonth = todayIso.slice(0, 7);
      setUpcomingBookings(activeBookings.filter((booking) => { const date = bookingDateInput(booking.event_date); return date >= todayIso && date.slice(0, 7) === currentMonth; }).sort((a, b) => { const dateDifference = bookingDateInput(a.event_date).localeCompare(bookingDateInput(b.event_date)); if (dateDifference) return dateDifference; const timeDifference = (a.start_time || "23:59").localeCompare(b.start_time || "23:59"); return timeDifference || a.event_name.localeCompare(b.event_name); }).slice(0, 5));
      const activePackages = (packagesRes.data || []) as Array<{ is_active: boolean }>;
      setReadiness({ businessData: (clientsRes.count || 0) > 0, packages: activePackages.some((item) => item.is_active), consumables: inventory.some((item) => item.is_active), calendar: false });
      setActionFlags({ overdueExpenses: expenses.filter((e) => e.status === "PENDING").length, lowStock, pendingBookings: activeBookings.length });
    })();
    return () => { cancelled = true; };
  }, [activeView, authUser]);
  useEffect(() => {
    if (activeView !== "Overview") return;
    let cancelled = false;
    void fetch("/api/google-calendar", { cache: "no-store" }).then((response) => response.json()).then((result) => {
      if (!cancelled) setReadiness((current) => ({ ...current, calendar: Boolean(result.connected) }));
    }).catch(() => { if (!cancelled) setReadiness((current) => ({ ...current, calendar: false })); });
    return () => { cancelled = true; };
  }, [activeView, authUser]);

  const healthChecks = [
    { key: "businessData" as const, label: "Client records", note: readiness.businessData ? "Client records are available" : "Add your first client", view: "Clients" },
    { key: "packages" as const, label: "Package catalog", note: readiness.packages ? "Active packages are ready" : "Create an active package", view: "Packages" },
    { key: "consumables" as const, label: "Consumables", note: readiness.consumables ? "Inventory items are available" : "Add inventory items", view: "Inventory" },
    { key: "calendar" as const, label: "Calendar integration", note: readiness.calendar ? "Calendar is connected" : "Integration is not connected", view: "Calendar" },
  ];
  const readyChecks = healthChecks.filter((item) => readiness[item.key]).length;

  if (!authChecked || !authUser) return <LoadingScreen label="Checking secure session" />;
  const workspaceNavigation = authUser.role === "ADMIN" ? [...navigation, ["Invoices", Receipt] as const] : navigation;

  return (
    <div className="app-shell">
      <DialogHost />
      <aside className={`sidebar ${sidebarOpen ? "mobile-open" : ""}`}>
        <div className="brand">
          <div className="brand-mark">S</div>
          <div>
            <strong>Salikha</strong>
            <span>Studio OS</span>
          </div>
        </div>

        <div className="workspace-label">Workspace</div>
        <nav aria-label="Main navigation">
          {workspaceNavigation.map(([label, Icon], index) => (
            <button className={`nav-item ${activeView === label || (activeView === "Overview" && index === 0) ? "active" : ""}`} key={label} type="button" onClick={() => { setActiveView(["Bookings", "Clients", "Calendar", "Cashflow", "Expenses", "Inventory", "Equipment", "Packages", "Reports", "Invoices"].includes(label) ? label : "Overview"); setSidebarOpen(false); }}>
              <Icon className="nav-icon" aria-hidden="true" />
              <span>{label}</span>
            </button>
          ))}
          {authUser.role === "ADMIN" && <button className={`nav-item ${activeView === "Team" ? "active" : ""}`} type="button" onClick={() => { setActiveView("Team"); setSidebarOpen(false); }}><Users className="nav-icon" aria-hidden="true" /><span>Team access</span></button>}
        </nav>

        <div className="sidebar-bottom">
          <button className={`nav-item ${activeView === "Settings" ? "active" : ""}`} type="button" onClick={() => { setActiveView("Settings"); setSidebarOpen(false); }}><Settings className="nav-icon" aria-hidden="true" /><span>Settings</span></button>
          <div className="user-chip">
            {authUser.profilePhoto ? <img className="avatar" src={authUser.profilePhoto} alt="" /> : <div className="avatar">{authUser.fullName.split(/\s+/).map((p)=>p[0]).slice(0,2).join("").toUpperCase()}</div>}
            <div><strong>{authUser.fullName}</strong><span>{authUser.role === "ADMIN" ? "Administrator" : "Team member"}</span></div>
            <button className="more" type="button" title="Sign out" aria-label="Sign out" onClick={async()=>{await fetch("/api/auth/logout",{method:"POST"});window.location.replace("/login")}}><LogOut aria-hidden="true" /><span>Sign out</span></button>
          </div>
        </div>
      </aside>

      {sidebarOpen && <button className="sidebar-overlay" type="button" onClick={() => setSidebarOpen(false)} aria-label="Close navigation" />}
      <main className="main-content">
        <header className="topbar">
          <div className="topbar-left"><button className="sidebar-toggle" type="button" onClick={() => setSidebarOpen((open) => !open)} aria-label={sidebarOpen ? "Close navigation" : "Open navigation"}><Menu aria-hidden="true" /></button><div className="breadcrumb"><span>Workspace</span><b>/</b><strong>{activeView}</strong></div></div>
          <div className="topbar-actions">
            <span className="system-status"><i /> System ready</span>
            <button className="icon-button" type="button" aria-label="Toggle dark mode" title={darkMode ? "Switch to light mode" : "Switch to dark mode"} onClick={toggleTheme}>{darkMode ? <Sun aria-hidden="true" /> : <Moon aria-hidden="true" />}</button>
            <div className="notification-wrap"><button className="icon-button notification-button" type="button" aria-label="Notifications" aria-expanded={notificationsOpen} onClick={() => setNotificationsOpen((open) => !open)}><Bell aria-hidden="true" />{actionFlags.pendingBookings + actionFlags.overdueExpenses + actionFlags.lowStock > 0 && <i />}</button>{notificationsOpen && <div className="notification-popover" role="dialog" aria-label="Notifications"><div className="notification-heading"><strong>Needs attention</strong><button className="close-button" type="button" aria-label="Close notifications" onClick={() => setNotificationsOpen(false)}><X aria-hidden="true" /></button></div>{[["Pending bookings", actionFlags.pendingBookings, "Bookings"], ["Pending expenses", actionFlags.overdueExpenses, "Expenses"], ["Low stock items", actionFlags.lowStock, "Inventory"]].map(([label, count, view]) => <button className="notification-row" type="button" key={label} onClick={() => { setActiveView(String(view)); setNotificationsOpen(false); }}><span><strong>{label}</strong><small>{Number(count) ? "Review items that need attention" : "All clear"}</small></span><b>{count}</b></button>)}</div>}</div>
            <button className="avatar small" type="button" aria-label="Open profile settings" onClick={()=>setActiveView("Settings")}>{authUser.fullName.split(/\s+/).map((p)=>p[0]).slice(0,2).join("").toUpperCase()}</button>
          </div>
        </header>

        <div className="page-content" key={activeView}>
           {activeView === "Team" && authUser.role === "ADMIN" ? <TeamView currentUser={authUser} /> : activeView === "Invoices" && authUser.role === "ADMIN" ? <InvoicesView /> : activeView === "Bookings" ? <BookingsView isAdmin={authUser.role === "ADMIN"} showForm={showBookingForm} onShowForm={setShowBookingForm} onNavigate={setActiveView} /> : activeView === "Clients" ? <ClientsView /> : activeView === "Calendar" ? <CalendarView /> : activeView === "Cashflow" ? <CashflowView /> : activeView === "Expenses" ? <ExpensesView /> : activeView === "Inventory" ? <InventoryView onNavigate={setActiveView} /> : activeView === "Equipment" ? <EquipmentView /> : activeView === "Packages" ? <PackagesView onNavigate={setActiveView} onCreateBooking={() => { setActiveView("Bookings"); setShowBookingForm(true); }} /> : activeView === "Reports" ? <ReportsView /> : activeView === "Settings" ? <SettingsView user={authUser} /> : <>
          <section className="page-heading">
            <div>
              <p className="eyebrow">{todayLabel}</p>
              <h1>{greeting}, Studio.</h1>
              <p className="subheading">Here is the operational pulse of your business.</p>
            </div>
            <button className="primary-button" type="button" onClick={() => { setActiveView("Bookings"); setShowBookingForm(true); }}><Plus aria-hidden="true" /> New booking</button>
          </section>

          <section className="metric-grid" aria-label="Business metrics">
            {(() => {
              const money = (value: number) => `₱${value.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`;
              return [
                ["Revenue this month", money(dashboardStats.revenue), dashboardStats.revenue === 0 ? "No posted inflows this month" : "Posted cash inflows", dashboardStats.revenue > 0 ? "positive" : "neutral"],
                ["Pending bookings", String(dashboardStats.bookings), dashboardStats.bookings === 0 ? "No bookings awaiting completion" : `${upcomingBookings.length} upcoming events`, "neutral"],
                ["Outstanding balance", money(dashboardStats.outstanding), dashboardStats.outstanding === 0 ? "All accounts settled" : "Unpaid booking balances", dashboardStats.outstanding === 0 ? "positive" : "warning"],
                ["Low stock items", String(dashboardStats.lowStock), dashboardStats.lowStock === 0 ? "Inventory is healthy" : "Below reorder level", dashboardStats.lowStock === 0 ? "positive" : "warning"],
              ].map(([label, value, note, tone]) => (
                <article className="metric-card" key={label}>
                  <div className="metric-top"><span>{loading ? <Skeleton className="label-skeleton" /> : label}</span><span className={`metric-dot ${tone}`} /></div>
                  {loading ? <Skeleton className="value-skeleton" /> : <strong>{value}</strong>}
                  {loading ? <Skeleton className="note-skeleton" /> : <small>{note}</small>}
                </article>
              ));
            })()}
          </section>

          {dashboardError && <p className="dashboard-data-error" role="alert">Some dashboard data could not be loaded: {dashboardError}</p>}

          <section className="content-grid">
            <article className="panel schedule-panel">
              <div className="panel-heading"><div><p className="eyebrow">Operations · This month</p><h2>Upcoming schedule</h2></div><button className="text-button" type="button" onClick={() => setActiveView("Calendar")}>View calendar <ArrowRight aria-hidden="true" /></button></div>
              {loading ? <div className="loading-list"><Skeleton /><Skeleton /><Skeleton /></div> : upcomingBookings.length === 0 ? <EmptyState title="Your schedule is clear" description="No future bookings are Pending." action="Create a booking" onAction={() => { setActiveView("Bookings"); setShowBookingForm(true); }} /> : <div className="upcoming-list">{upcomingBookings.map((booking) => <article className="upcoming-card" key={booking.id}><div className="upcoming-card-header"><div><small>Event</small><strong>{booking.event_name}</strong><span>{booking.clients?.display_name || "Unknown client"}</span></div><div className="upcoming-balance"><small>Outstanding balance</small><strong>₱{Math.max(0, Number(booking.total_amount || 0) - Number(booking.downpayment_amount || 0)).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong><span className="upcoming-status">Pending</span></div></div><div className="upcoming-card-details"><div><small>Package</small><strong>{booking.service_packages?.name || "Custom service"}</strong></div><div><small>Date</small><strong>{bookingDateLabel(booking.event_date)}</strong></div><div><small>Time</small><strong>{booking.start_time ? bookingTimeLabel(booking.start_time) : "Not set"} – {booking.end_time ? bookingTimeLabel(booking.end_time) : "Not set"}</strong></div><div><small>Venue</small><strong>{booking.venue || "Not set"}</strong>{booking.maps_url && <a className="booking-map-link" href={booking.maps_url} target="_blank" rel="noreferrer"><MapPin aria-hidden="true" /> Open map</a>}</div></div></article>)}</div>}
            </article>
            <article className="panel attention-panel">
              <div className="panel-heading"><div><p className="eyebrow">Needs attention</p><h2>Action queue</h2></div><span className="count-badge">{actionFlags.overdueExpenses + actionFlags.lowStock + actionFlags.pendingBookings}</span></div>
              {loading ? <div className="loading-list"><Skeleton /><Skeleton /></div> : actionFlags.overdueExpenses + actionFlags.lowStock + actionFlags.pendingBookings === 0 ? <EmptyState title="Nothing needs your attention" description="Payment reviews, low stock warnings, and unclosed events will be listed here." /> : <div className="action-list">{[{ title: "Pending bookings", note: "Review and update event status", count: actionFlags.pendingBookings, view: "Bookings", icon: ClipboardList }, { title: "Pending expenses", note: "Review items awaiting payment", count: actionFlags.overdueExpenses, view: "Expenses", icon: Receipt }, { title: "Low stock items", note: "Restock items below reorder level", count: actionFlags.lowStock, view: "Inventory", icon: Boxes }].filter((item) => item.count > 0).map((item) => <button type="button" key={item.view} onClick={() => setActiveView(item.view)}><span className="action-icon"><item.icon aria-hidden="true" /></span><span className="action-copy"><strong>{item.title}</strong><small>{item.note}</small></span><b>{item.count}</b><ArrowRight className="action-arrow" aria-hidden="true" /></button>)}</div>}
            </article>
          </section>

          <section className="dashboard-detail-grid"><article className="panel dashboard-pulse"><div className="panel-heading"><div><p className="eyebrow">Financial pulse</p><h2>Where the business stands</h2></div><button className="text-button" type="button" onClick={() => setActiveView("Reports")}>Open reports <ArrowRight aria-hidden="true" /></button></div><div className="pulse-list"><button type="button" onClick={() => setActiveView("Cashflow")}><span className="pulse-icon inflow"><TrendingUp aria-hidden="true" /></span><span><strong>Verified cash balance</strong><small>Opening balances + posted transactions</small></span><b>{`₱${dashboardStats.verifiedCash.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`}</b><ArrowRight aria-hidden="true" /></button><button type="button" onClick={() => setActiveView("Expenses")}><span className="pulse-icon outflow"><TrendingDown aria-hidden="true" /></span><span><strong>Paid expenses this month</strong><small>Posted outflows this month</small></span><b>{`₱${dashboardStats.paidExpenses.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`}</b><ArrowRight aria-hidden="true" /></button><button type="button" onClick={() => setActiveView("Bookings")}><span className="pulse-icon neutral"><CircleDollarSign aria-hidden="true" /></span><span><strong>Outstanding receivables</strong><small>Pending totals less downpayments</small></span><b>{`₱${dashboardStats.outstanding.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`}</b><ArrowRight aria-hidden="true" /></button></div></article><article className="panel dashboard-health"><div className="panel-heading"><div><p className="eyebrow">System health</p><h2>Setup checklist</h2></div><span className="count-badge">{readyChecks} / {healthChecks.length}</span></div><div className="health-list">{healthChecks.map((item) => <button type="button" key={item.key} onClick={() => setActiveView(item.view)}><span><i className={`health-dot ${readiness[item.key] ? "ready" : "pending"}`} /><strong>{item.label}</strong><small>{item.note}</small></span><ArrowRight aria-hidden="true" /></button>)}</div></article></section>

          {(!readiness.businessData || !readiness.packages || !readiness.consumables) && <section className="setup-banner">
            <div className="setup-icon"><Sparkles aria-hidden="true" /></div>
            <div><p className="eyebrow">Finish local setup</p><h2>Add missing operational records.</h2><p>Set up clients, active packages, and inventory in the local workspace to make operations and analytics useful.</p></div>
            <button className="secondary-button" type="button" onClick={() => setActiveView("Settings")}>Open setup <ArrowRight aria-hidden="true" /></button>
          </section>}

          <footer className="footer"><span>Salikha Studio OS · Internal use only</span><span>Data source: connected PostgreSQL database</span></footer>
          </>}
        </div>
      </main>
    </div>
  );
}

function EmptyState({ title, description, action, onAction }: { title: string; description: string; action?: string; onAction?: () => void }) {
  return <div className="empty-state"><div className="empty-mark"><CalendarDays aria-hidden="true" /></div><h3>{title}</h3><p>{description}</p>{action && <button className="secondary-button" type="button" onClick={onAction}>{action} <ArrowRight aria-hidden="true" /></button>}</div>;
}

type BookingPreparation = { layoutReady: boolean; venueReady: boolean; backdropColor: string | null };
type BookingRecord = { id: string; client: string; eventName: string; date: string; startTime: string; endTime: string; venue: string; mapsUrl?: string; packageId?: string | null; packageName: string; total: number; paidAmount?: number; downpaymentAmount?: number; notes?: string; status: "PENDING" | "DONE" | "CANCELLED"; preparation: BookingPreparation };
type LocalDbBookingRow = { id: string; package_id: string | null; event_name: string; event_date: string; start_time: string | null; end_time: string | null; venue: string | null; maps_url: string | null; total_amount: number; paid_amount: number; downpayment_amount: number; status: "PENDING" | "DONE" | "CANCELLED"; preparation_layout_ready: boolean; preparation_venue_ready: boolean; preparation_backdrop_color: string | null; clients: { display_name: string; maps_url?: string | null } | null; service_packages: { name: string } | null; };
function manilaDateKey(date: Date) { const parts = new Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Manila", year: "numeric", month: "2-digit", day: "2-digit" }).formatToParts(date); const part = (type: string) => parts.find((entry) => entry.type === type)?.value || ""; return `${part("year")}-${part("month")}-${part("day")}`; }
function bookingDateInput(value: string) { if (/^\d{4}-\d{2}-\d{2}$/.test(value)) return value; const date = new Date(value); if (Number.isNaN(date.getTime())) return value.slice(0, 10); return manilaDateKey(date); }
function bookingTimeInput(value: string | null) { return value ? (/^\d{1,2}:\d{2}/.exec(value)?.[0] || "").padStart(5, "0") : ""; }
function bookingDateLabel(value: string) { const normalized = bookingDateInput(value); const date = new Date(`${normalized}T12:00:00+08:00`); return Number.isNaN(date.getTime()) ? normalized : new Intl.DateTimeFormat("en-PH", { timeZone: "Asia/Manila", day: "numeric", month: "short", year: "numeric" }).format(date); }
function bookingTimeLabel(value: string) { return formatManilaTime(value) === "—" ? "Time not set" : formatManilaTime(value); }
function normalizeBookingStatus(status: string): BookingRecord["status"] { if (status === "CANCELLED") return "CANCELLED"; if (status === "DONE" || status === "COMPLETED") return "DONE"; return "PENDING"; }

function BookingsView({ isAdmin, showForm, onShowForm, onNavigate }: { isAdmin: boolean; showForm: boolean; onShowForm: (value: boolean) => void; onNavigate: (view: string) => void }) {
  const [bookings, setBookings] = useState<BookingRecord[]>([]); const [editingBooking, setEditingBooking] = useState<BookingRecord | null>(null); const [reconcilingBooking, setReconcilingBooking] = useState<BookingRecord | null>(null); const [preparingBooking, setPreparingBooking] = useState<BookingRecord | null>(null); const [statusFilter, setStatusFilter] = useState("All statuses"); const [dateFilter, setDateFilter] = useState("All dates"); const [query, setQuery] = useState(""); const today = new Date(); const todayKey = `${today.getFullYear()}-${String(today.getMonth() + 1).padStart(2, "0")}`;
  useEffect(() => { let cancelled = false; const loadBookings = async () => { const database = getLocalDatabase(); if (database) { const detailedColumns = "id, package_id, event_name, event_date, start_time, end_time, venue, maps_url, total_amount, paid_amount, downpayment_amount, status, preparation_layout_ready, preparation_venue_ready, preparation_backdrop_color, clients(display_name, maps_url), service_packages(name)"; const coreColumns = "id, package_id, event_name, event_date, start_time, end_time, venue, maps_url, total_amount, paid_amount, downpayment_amount, status, clients(display_name, maps_url), service_packages(name)"; const minimalColumns = "id, event_name, event_date, start_time, end_time, venue, total_amount, paid_amount, downpayment_amount, status"; let result = await database.from("bookings").select(detailedColumns).order("event_date"); if (result.error) result = await database.from("bookings").select(coreColumns).order("event_date"); if (result.error) result = await database.from("bookings").select(minimalColumns).order("event_date"); const { data, error } = result; if (!error && data && !cancelled) { const loaded = (data as unknown as LocalDbBookingRow[]).map((booking) => ({ id: booking.id, packageId: booking.package_id || null, client: booking.clients?.display_name || "Unknown client", eventName: booking.event_name, date: bookingDateInput(booking.event_date), startTime: bookingTimeInput(booking.start_time), endTime: bookingTimeInput(booking.end_time), venue: booking.venue || "", mapsUrl: booking.maps_url || booking.clients?.maps_url || "", packageName: booking.service_packages?.name || "", total: Number(booking.total_amount), paidAmount: Number(booking.paid_amount || 0), downpaymentAmount: Number(booking.downpayment_amount || 0), status: normalizeBookingStatus(booking.status), preparation: { layoutReady: Boolean(booking.preparation_layout_ready), venueReady: Boolean(booking.preparation_venue_ready), backdropColor: booking.preparation_backdrop_color || null } })); setBookings(loaded); window.localStorage.setItem("salikha-bookings", JSON.stringify(loaded)); return; } } const stored = window.localStorage.getItem("salikha-bookings"); if (stored && !cancelled) { const normalized = (JSON.parse(stored) as Array<BookingRecord & { status: string }>).map((booking) => ({ ...booking, date: bookingDateInput(booking.date), startTime: bookingTimeInput(booking.startTime), endTime: bookingTimeInput(booking.endTime), paidAmount: Number(booking.paidAmount || 0), downpaymentAmount: Number(booking.downpaymentAmount || booking.paidAmount || 0), status: normalizeBookingStatus(booking.status), preparation: booking.preparation || { layoutReady:false, venueReady:false, backdropColor:null } })); setBookings(normalized); window.localStorage.setItem("salikha-bookings", JSON.stringify(normalized)); } }; void loadBookings(); return () => { cancelled = true; }; }, []);
  const upcomingBookings = bookings.filter((booking) => booking.status === "PENDING" && booking.date >= manilaDateKey(today));
  const visibleBookings = bookings.filter((booking) => (statusFilter === "All statuses" || booking.status === statusFilter.toUpperCase()) && (!query || `${booking.client} ${booking.eventName} ${booking.venue}`.toLowerCase().includes(query.toLowerCase())) && (dateFilter === "All dates" || (dateFilter === "This month" && booking.date.slice(0, 7) === todayKey) || (dateFilter === "Next 30 days" && new Date(booking.date) >= today && new Date(booking.date) <= new Date(today.getTime() + 30 * 86400000)))).sort((a, b) => { const statusOrder: Record<string, number> = { PENDING: 0, CANCELLED: 1, DONE: 2 }; const statusDifference = (statusOrder[a.status] ?? 1) - (statusOrder[b.status] ?? 1); if (statusDifference) return statusDifference; const dateDifference = a.date.localeCompare(b.date); if (dateDifference) return dateDifference; const timeDifference = (a.startTime || "23:59").localeCompare(b.startTime || "23:59"); return timeDifference || a.eventName.localeCompare(b.eventName); });
  const markBookingDone = async (booking: BookingRecord) => {
    const response = await fetch("/api/booking-update", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ id: booking.id, eventName: booking.eventName, date: booking.date, startTime: booking.startTime, endTime: booking.endTime, venue: booking.venue, mapsUrl: booking.mapsUrl, packageName: booking.packageName, total: booking.total, downpaymentAmount: booking.downpaymentAmount ?? booking.paidAmount ?? 0, status: "DONE" }) });
    const result = await response.json();
    if (!response.ok) { await showNotice(result.error || "Could not mark booking as done.", "Could not update booking"); return; }
    const updated = { ...booking, status: "DONE" as const, paidAmount: Number(result.booking.paidAmount), downpaymentAmount: Number(result.booking.downpaymentAmount) };
    setBookings((current) => { const next = current.map((item) => item.id === booking.id ? updated : item); window.localStorage.setItem("salikha-bookings", JSON.stringify(next)); return next; });
    setReconcilingBooking(updated);
  };
  return <>
    <section className="page-heading booking-heading">
      <div><p className="eyebrow">Workspace / Bookings</p><h1>Bookings</h1><p className="subheading">Keep every event, payment status, and operational detail in one place.</p></div>
      <div className="heading-actions"><button className="secondary-button" type="button" onClick={() => onNavigate("Calendar")}><CalendarDays aria-hidden="true" /> Calendar</button><button className="primary-button" type="button" onClick={() => onShowForm(true)}><Plus aria-hidden="true" /> New booking</button></div>
    </section>
    <section className="booking-summary-grid">
      <div className="booking-summary-card"><span>All bookings</span><strong>{bookings.length}</strong><small>Saved in this workspace</small></div>
      <div className="booking-summary-card"><span>Pending bookings</span><strong>{bookings.filter((booking) => booking.status === "PENDING").length}</strong><small>Needs a decision</small></div>
      <div className="booking-summary-card"><span>Upcoming events</span><strong>{upcomingBookings.length}</strong><small>Future bookings still Pending</small></div>
      <div className="booking-summary-card accent-card"><span>Outstanding balance</span><strong>₱{bookings.filter((booking) => booking.status === "PENDING").reduce((sum, booking) => sum + Math.max(0, booking.total - Number(booking.downpaymentAmount ?? booking.paidAmount ?? 0)), 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong><small>Pending bookings, less downpayment</small></div>
    </section>
    <section className="booking-toolbar" aria-label="Booking filters">
      <div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} aria-label="Search bookings" placeholder="Search client, venue, or event" /></div>
      <button className="filter-button" type="button"><CalendarDays aria-hidden="true" /> Date <Chevron /></button>
       <label className="visually-hidden" htmlFor="booking-status-filter">Booking status</label><select className="filter-select" id="booking-status-filter" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option>All statuses</option><option>Pending</option><option>Done</option><option>Cancelled</option></select>
      <label className="visually-hidden" htmlFor="booking-date-filter">Booking date range</label><select className="filter-select" id="booking-date-filter" value={dateFilter} onChange={(event) => setDateFilter(event.target.value)}><option>This month</option><option>Next 30 days</option><option>All dates</option></select>
    </section>
    <section className="booking-table-panel panel">
       <div className="panel-heading"><div><p className="eyebrow">Register</p><h2>All bookings</h2></div><span className="muted-label">{visibleBookings.length} records</span></div>
      <div className="booking-table-head"><span>Event</span><span>Schedule</span><span>Client / venue</span><span>Package</span><span>Amount</span><span>Status</span><span>Action</span></div>
       {visibleBookings.length === 0 ? <div className="booking-empty"><div className="empty-mark"><ClipboardList aria-hidden="true" /></div><h3>{bookings.length ? "No matching bookings" : "No bookings yet"}</h3><p>{bookings.length ? "Try another status filter." : "Once your data is connected, every booking will be searchable and sortable here."}</p><button className="secondary-button" type="button" onClick={() => onShowForm(true)}><Plus aria-hidden="true" /> {bookings.length ? "Create booking" : "Create first booking"}</button></div> : <div className="booking-list">{visibleBookings.map((booking) => { const ready = booking.preparation.layoutReady && booking.preparation.venueReady && Boolean(booking.preparation.backdropColor); return <div className="booking-row" key={booking.id}><div className="booking-event-wrapper"><button className="booking-event-cell" type="button" onClick={() => setEditingBooking(booking)}><strong>{booking.eventName}</strong><small>{booking.client}</small></button>{isAdmin && booking.status === "PENDING" && <button className={`booking-preparation-status ${ready ? "ready" : "pending"}`} type="button" onClick={() => setPreparingBooking(booking)}><i />{ready ? "Ready for Deployment" : "To Prepare"}</button>}</div><span>{bookingDateLabel(booking.date)}<small>{booking.startTime ? bookingTimeLabel(booking.startTime) : "Time not set"}{booking.endTime ? ` – ${bookingTimeLabel(booking.endTime)}` : ""}</small></span><span>{booking.venue || "Venue not set"}{booking.mapsUrl && <a className="booking-map-link" href={booking.mapsUrl} target="_blank" rel="noreferrer" aria-label={`Open map for ${booking.venue || booking.eventName}`}><MapPin aria-hidden="true" /> Maps</a>}</span><span>{booking.packageName || "Custom service"}</span><span>₱{booking.total.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span><button className={`booking-status-button status-pill ${booking.status.toLowerCase()}`} type="button" disabled={booking.status !== "PENDING"} onClick={() => void markBookingDone(booking)} aria-label={booking.status === "PENDING" ? `Mark ${booking.eventName} as done and paid` : `Booking ${booking.status.toLowerCase()}`}>{booking.status === "PENDING" ? "Mark done" : booking.status}</button><button className="booking-row-action" type="button" onClick={() => setEditingBooking(booking)}>Edit</button></div>; })}</div>}
    </section>
    {showForm && <BookingForm onClose={() => onShowForm(false)} onSave={async (booking) => {
      const database = getLocalDatabase();
      if (database) {
        let clientId = (await database.from("clients").select("id").eq("display_name", booking.client).maybeSingle()).data?.id;
        if (!clientId) clientId = (await database.from("clients").insert({ display_name: booking.client }).select("id").single()).data?.id;
        const packageId = booking.packageId || (await database.from("service_packages").select("id").eq("name", booking.packageName).maybeSingle()).data?.id;
        if (clientId) {
          const result = await database.from("bookings").insert({ client_id: clientId, package_id: packageId || null, event_name: booking.eventName, event_date: booking.date, start_time: booking.startTime, end_time: booking.endTime, venue: booking.venue || null, maps_url: booking.mapsUrl || null, total_amount: booking.total, downpayment_amount: booking.downpaymentAmount || 0, paid_amount: booking.downpaymentAmount || 0, notes: booking.notes || null }).select("id, status").single();
          if (result.error) throw new Error(result.error.message);
          if (result.data) booking = { ...booking, id: result.data.id, packageId: packageId || null, status: result.data.status as BookingRecord["status"] };
          if (result.data) void fetch("/api/google-calendar/sync", { method: "POST" }).catch(() => undefined);
        }
      }
      setBookings((current) => { const next = [booking, ...current]; window.localStorage.setItem("salikha-bookings", JSON.stringify(next)); return next; }); onShowForm(false);
    }} />}
    {editingBooking && <BookingEditForm booking={editingBooking} onClose={() => setEditingBooking(null)} onSave={async (updated) => {
      const response = await fetch("/api/booking-update", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ id: updated.id, eventName: updated.eventName, date: updated.date, startTime: updated.startTime, endTime: updated.endTime, venue: updated.venue, mapsUrl: updated.mapsUrl, packageName: updated.packageName, total: updated.total, downpaymentAmount: updated.downpaymentAmount ?? 0, status: updated.status }) });
      const result = await response.json(); if (!response.ok) throw new Error(result.error || "Could not save booking.");
      updated = { ...updated, paidAmount: Number(result.booking.paidAmount), downpaymentAmount: Number(result.booking.downpaymentAmount) };
      setBookings((current) => { const next = current.map((item) => item.id === updated.id ? updated : item); window.localStorage.setItem("salikha-bookings", JSON.stringify(next)); return next; });
      setEditingBooking(null); if (updated.status === "DONE") setReconcilingBooking(updated);
    }} />}
    {preparingBooking && <BookingPreparationForm bookingName={preparingBooking.eventName} preparation={preparingBooking.preparation} onClose={() => setPreparingBooking(null)} onSave={async (preparation) => { const response=await fetch("/api/booking-preparation",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id:preparingBooking.id,layoutReady:preparation.layoutReady,venueReady:preparation.venueReady,backdropColor:preparation.backdropColor})}); const result=await response.json(); if(!response.ok)throw new Error(result.error||"Could not save preparation."); const updated={...preparingBooking,preparation:result.preparation as BookingPreparation}; setBookings(current=>{const next=current.map(item=>item.id===updated.id?updated:item);window.localStorage.setItem("salikha-bookings",JSON.stringify(next));return next});setPreparingBooking(null); }} />}
    {reconcilingBooking && <BookingReconciliationForm bookingId={reconcilingBooking.id} packageId={reconcilingBooking.packageId || null} bookingName={reconcilingBooking.eventName} onClose={() => setReconcilingBooking(null)} onSave={async (closeout) => { const response = await fetch("/api/booking-consumption", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ bookingId: reconcilingBooking.id, ...closeout }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Could not record actual usage."); setReconcilingBooking(null); }} />}
  </>;
}

function Chevron() { return <span className="chevron" aria-hidden="true">⌄</span>; }

function BookingForm({ onClose, onSave }: { onClose: () => void; onSave: (booking: BookingRecord) => void | Promise<void> }) {
  const [client, setClient] = useState(""); const [availableClients, setAvailableClients] = useState<ClientRecord[]>([]); const [eventName, setEventName] = useState(""); const [date, setDate] = useState(""); const [startTime, setStartTime] = useState(""); const [endTime, setEndTime] = useState(""); const [venue, setVenue] = useState(""); const [mapsUrl, setMapsUrl] = useState(""); const [packageName, setPackageName] = useState(""); const [packageOverrideReason, setPackageOverrideReason] = useState(""); const [basePrice, setBasePrice] = useState(""); const [transport, setTransport] = useState(""); const [addons, setAddons] = useState(""); const [downpayment, setDownpayment] = useState(""); const [notes, setNotes] = useState("");
  const [availablePackages, setAvailablePackages] = useState<PackageRecord[]>([]);
  const [packageDetails, setPackageDetails] = useState<{ duration: string | null; inclusions: string | null; recipe: Array<{ itemName: string; quantity: string; unit: string; lineCost: string }>; recipeCost: number } | null>(null);
  const [packageLoading, setPackageLoading] = useState(false); const [packageError, setPackageError] = useState(""); const [saving, setSaving] = useState(false);
  useEffect(() => { let cancelled = false; const loadPackages = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("service_packages").select("id, name, description, duration, inclusions, notes, base_price, is_active").order("name"); if (!error && data && !cancelled) { const items = (data as LocalDbPackageRow[]).map((item) => ({ id: item.id, name: item.name, description: item.description || "", duration: item.duration || "", inclusions: item.inclusions || "", notes: item.notes || "", basePrice: Number(item.base_price), active: item.is_active })); setAvailablePackages(items); window.localStorage.setItem("salikha-packages", JSON.stringify(items)); return; } } const stored = window.localStorage.getItem("salikha-packages"); if (stored && !cancelled) setAvailablePackages(JSON.parse(stored) as PackageRecord[]); }; void loadPackages(); return () => { cancelled = true; }; }, []);
  useEffect(() => { let cancelled = false; const loadClients = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("clients").select("id, display_name, email, facebook_url, phone, address, notes").order("display_name"); if (!error && data && !cancelled) { setAvailableClients((data as LocalDbClientRow[]).map((item) => ({ id: item.id, name: item.display_name, messenger: item.facebook_url || "", phone: item.phone || "", email: item.email || "", address: item.address || "", notes: item.notes || "" }))); return; } } const clients = window.localStorage.getItem("salikha-clients"); if (clients && !cancelled) setAvailableClients(JSON.parse(clients) as ClientRecord[]); }; void loadClients(); return () => { cancelled = true; }; }, []);
  const selectedPackage = availablePackages.find((item) => item.name === packageName);
  useEffect(() => { if (!selectedPackage || packageName === "Custom service") { setPackageDetails(null); setPackageError(""); setPackageLoading(false); return; } let cancelled = false; setPackageLoading(true); setPackageError(""); fetch(`/api/package-recipe?packageId=${encodeURIComponent(selectedPackage.id)}`, { cache: "no-store" }).then(async (response) => { const result = await response.json(); if (!response.ok) throw new Error(result.error || "Could not load package details."); if (!cancelled) { const recipe = result.data as Array<{ itemName: string; quantity: string; unit: string; lineCost: string }>; setPackageDetails({ duration: result.package?.duration || null, inclusions: result.package?.inclusions || null, recipe, recipeCost: recipe.reduce((sum, line) => sum + Number(line.lineCost || 0), 0) }); } }).catch((cause: unknown) => { if (!cancelled) setPackageError(cause instanceof Error ? cause.message : "Could not load package details."); }).finally(() => { if (!cancelled) setPackageLoading(false); }); return () => { cancelled = true; }; }, [selectedPackage, packageName]);
  const total = Math.max(0, (Number(basePrice) || 0) + (Number(transport) || 0) + (Number(addons) || 0));
  const pendingBalance = Math.max(0, total - (Number(downpayment) || 0));
  const estimatedProfit = total - (packageDetails?.recipeCost || 0);
  return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="booking-form-title"><form className="booking-drawer" onSubmit={async (event) => { event.preventDefault(); if (saving || !client.trim() || !eventName.trim() || !date || !startTime || !endTime || !packageName.trim() || Number(downpayment) > total || (mapsUrl && !/^https?:\/\//i.test(mapsUrl)) || (selectedPackage && Number(basePrice) !== selectedPackage.basePrice && !packageOverrideReason.trim())) return; setSaving(true); try { await onSave({ id: crypto.randomUUID(), client: client.trim(), eventName: eventName.trim(), date, startTime, endTime, venue: venue.trim(), mapsUrl: mapsUrl.trim(), packageId: selectedPackage?.id || null, packageName: packageName.trim(), total, paidAmount: Number(downpayment) || 0, downpaymentAmount: Number(downpayment) || 0, notes: notes.trim(), status: "PENDING", preparation: { layoutReady: false, venueReady: false, backdropColor: null } }); } finally { setSaving(false); } }}>
    <div className="drawer-header"><div><p className="eyebrow">New record</p><h2 id="booking-form-title">Create booking</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close booking form"><X aria-hidden="true" /></button></div>
    <div className="drawer-body">
      <div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Event details</strong><small>Client, schedule, and venue are required.</small></div></div><div className="form-grid"><label>Client<select value={client} onChange={(event) => setClient(event.target.value)} required><option value="">Select a client</option>{availableClients.map((item) => <option key={item.id} value={item.name}>{item.name}</option>)}</select></label><label>Event name<input value={eventName} onChange={(event) => setEventName(event.target.value)} placeholder="e.g. Birthday celebration" required /></label><label>Event date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} required /></label><label>Start time<input type="time" value={startTime} onChange={(event) => setStartTime(event.target.value)} required /></label><label>End time<input type="time" value={endTime} onChange={(event) => setEndTime(event.target.value)} required /></label><label className="wide-field">Venue<input value={venue} onChange={(event) => setVenue(event.target.value)} placeholder="Enter event venue" /></label><label className="wide-field">Google Maps link<div className="input-with-icon"><MapPin aria-hidden="true" /><input type="url" value={mapsUrl} onChange={(event) => setMapsUrl(event.target.value)} placeholder="Paste Maps link (optional)" /></div></label></div></div>
      <div className="form-section"><div className="form-section-title"><span className="step-number">2</span><div><strong>Package and pricing</strong><small>Choose a package, then override the price only when needed.</small></div></div><div className="form-grid"><label className="wide-field">Package or service<select value={packageName} onChange={(event) => { setPackageName(event.target.value); setPackageOverrideReason(""); const item = availablePackages.find((entry) => entry.name === event.target.value); if (item) setBasePrice(String(item.basePrice)); else if (event.target.value === "Custom service") setBasePrice(""); }} required><option value="" disabled>{availablePackages.length ? "Select an active package" : "No active packages yet"}</option>{availablePackages.filter((item) => item.active).map((item) => <option value={item.name} key={item.id}>{item.name} — ₱{item.basePrice.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</option>)}<option value="Custom service">Custom service</option></select></label><label>Base price<div className="money-input"><span>₱</span><input value={basePrice} onChange={(event) => setBasePrice(event.target.value)} inputMode="decimal" placeholder="0.00" /></div></label><label>Add-ons<div className="money-input"><span>₱</span><input value={addons} onChange={(event) => setAddons(event.target.value)} inputMode="decimal" placeholder="0.00" /></div></label><label>Transportation<div className="money-input"><span>₱</span><input value={transport} onChange={(event) => setTransport(event.target.value)} inputMode="decimal" placeholder="0.00" /></div></label><label>Downpayment<div className="money-input"><span>₱</span><input value={downpayment} onChange={(event) => setDownpayment(event.target.value)} type="number" min="0" max={total} step="0.01" placeholder="0.00" /></div><small>Remaining balance while Pending: ₱{pendingBalance.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</small></label><label className="wide-field">Price override reason<input value={packageOverrideReason} onChange={(event) => setPackageOverrideReason(event.target.value)} placeholder="Required when changing the package default" /></label></div>
        {packageLoading ? <div className="package-loading-note"><Boxes aria-hidden="true" /><span>Loading package details and recipe…</span></div> : packageError ? <div className="package-loading-note package-error" role="alert">{packageError}</div> : selectedPackage && packageDetails ? <div className="package-loading-note package-details"><div><strong>{packageDetails.duration || "Duration not specified"}</strong><span>{packageDetails.inclusions || "No inclusions configured."}</span><strong>Recipe</strong>{packageDetails.recipe.length ? <ul>{packageDetails.recipe.map((line, index) => <li key={`${line.itemName}-${index}`}>{line.itemName}: {line.quantity} {line.unit} · ₱{Number(line.lineCost).toFixed(2)}</li>)}</ul> : <span>No consumables configured.</span>}<span>Recipe cost: ₱{packageDetails.recipeCost.toLocaleString("en-PH", { minimumFractionDigits: 2 })} · Estimated profit: ₱{estimatedProfit.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span></div></div> : <div className="package-loading-note"><Boxes aria-hidden="true" /><span>{packageName === "Custom service" ? "Custom service has no saved package recipe." : "Select a package to load its inclusions, duration, recipe, and estimated cost/profit."}</span></div>}
        <div className="price-preview"><span>Booking total</span><strong>₱{total.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div></div>
      <div className="form-section"><div className="form-section-title"><span className="step-number">3</span><div><strong>Internal notes</strong><small>Visible only to the studio team.</small></div></div><div className="form-grid"><label className="wide-field">Notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Add context for the team..." rows={4} /></label></div></div>
    </div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose} disabled={saving}>Cancel</button><button className="primary-button" type="submit" disabled={saving}><Check aria-hidden="true" /> {saving ? "Saving…" : "Save as draft"}</button></div>
  </form></div>;
}

type ClientBookingSummary = { id: string; eventName: string; date: string; total: number; downpayment: number; status: "PENDING" | "DONE" | "CANCELLED"; };
type ClientRecord = { id: string; name: string; messenger: string; phone: string; email: string; address: string; notes: string; bookingCount?: number; bookingHistory?: ClientBookingSummary[]; upcomingBookings?: ClientBookingSummary[]; outstandingBalance?: number; };
type LocalDbClientRow = { id: string; display_name: string; email: string | null; facebook_url: string | null; phone: string | null; address: string | null; notes: string | null; };
type LocalDbClientBookingRow = { id: string; client_id: string; event_name: string; event_date: string; total_amount: number; downpayment_amount: number | null; paid_amount: number | null; status: "PENDING" | "DONE" | "CANCELLED"; };

function ClientsView() {
  const [clients, setClients] = useState<ClientRecord[]>([]);
  const [selected, setSelected] = useState<ClientRecord | null>(null);
  const [showForm, setShowForm] = useState(false);
  const [editing, setEditing] = useState<ClientRecord | null>(null);
  const [query, setQuery] = useState("");
  useEffect(() => { let cancelled = false; const loadClients = async () => { const database = getLocalDatabase(); if (database) { const [clientResult, bookingResult] = await Promise.all([database.from("clients").select("id, display_name, email, facebook_url, phone, address, notes").order("display_name"), database.from("bookings").select("id, client_id, event_name, event_date, total_amount, downpayment_amount, paid_amount, status").order("event_date")]); if (!clientResult.error && !bookingResult.error && clientResult.data && bookingResult.data && !cancelled) { const today = manilaDateKey(new Date()); const bookingsByClient = new Map<string, LocalDbClientBookingRow[]>(); for (const booking of bookingResult.data as LocalDbClientBookingRow[]) { const rows = bookingsByClient.get(booking.client_id) ?? []; rows.push(booking); bookingsByClient.set(booking.client_id, rows); } const loaded = (clientResult.data as LocalDbClientRow[]).map((client) => { const bookings = bookingsByClient.get(client.id) ?? []; const activeBookings = bookings.filter((booking) => booking.status !== "CANCELLED"); const history = activeBookings.map((booking) => ({ id: booking.id, eventName: booking.event_name, date: bookingDateInput(booking.event_date), total: Number(booking.total_amount || 0), downpayment: Number(booking.downpayment_amount ?? booking.paid_amount ?? 0), status: booking.status })); return { id: client.id, name: client.display_name, messenger: client.facebook_url || "", phone: client.phone || "", email: client.email || "", address: client.address || "", notes: client.notes || "", bookingCount: activeBookings.length, bookingHistory: history, upcomingBookings: history.filter((booking) => booking.status === "PENDING" && booking.date >= today), outstandingBalance: bookings.filter((booking) => booking.status === "PENDING").reduce((sum, booking) => sum + Math.max(0, Number(booking.total_amount || 0) - Number(booking.downpayment_amount ?? booking.paid_amount ?? 0)), 0) }; }); setClients(loaded); window.localStorage.setItem("salikha-clients", JSON.stringify(loaded)); return; } } const stored = window.localStorage.getItem("salikha-clients"); if (stored && !cancelled) { const legacy = JSON.parse(stored) as Array<Partial<ClientRecord> & Pick<ClientRecord, "id" | "name" | "messenger" | "phone" | "email" | "address" | "notes">>; setClients(legacy.map((client) => ({ ...client, bookingCount: client.bookingCount ?? 0, bookingHistory: client.bookingHistory ?? [], upcomingBookings: client.upcomingBookings ?? [], outstandingBalance: client.outstandingBalance ?? 0 }))); } }; void loadClients(); return () => { cancelled = true; }; }, []);
  const visibleClients = clients.filter((client) => `${client.name} ${client.phone} ${client.email}`.toLowerCase().includes(query.toLowerCase()));

  return <>
    <section className="page-heading booking-heading"><div><p className="eyebrow">Workspace / Clients</p><h1>Clients</h1><p className="subheading">Keep conversations, bookings, and client context connected.</p></div><button className="primary-button" type="button" onClick={() => { setEditing(null); setShowForm(true); }}><Plus aria-hidden="true" /> Add client</button></section>
    <section className="booking-summary-grid client-summary-grid"><div className="booking-summary-card"><span>Total clients</span><strong>{clients.length}</strong><small>Saved in this workspace</small></div><div className="booking-summary-card"><span>Active conversations</span><strong>{clients.filter((client) => client.messenger).length}</strong><small>Messenger links available</small></div><div className="booking-summary-card"><span>With upcoming booking</span><strong>{clients.filter((client) => (client.upcomingBookings?.length ?? 0) > 0).length}</strong><small>Future, non-cancelled events</small></div><div className="booking-summary-card accent-card"><span>Outstanding balance</span><strong>₱{clients.reduce((sum, client) => sum + (client.outstandingBalance ?? 0), 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong><small>Pending booking balances after downpayment</small></div></section>
    <section className="booking-toolbar" aria-label="Client filters"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} aria-label="Search clients" placeholder="Search by name, phone, or email" /></div><span className="filter-context"><Users aria-hidden="true" /> All clients</span></section>
    <section className="client-workspace"><div className="booking-table-panel panel client-register"><div className="panel-heading"><div><p className="eyebrow">Directory</p><h2>All clients</h2></div><span className="muted-label">{visibleClients.length} records</span></div>{visibleClients.length === 0 ? <div className="booking-empty"><div className="empty-mark"><Users aria-hidden="true" /></div><h3>{clients.length ? "No matching clients" : "No clients yet"}</h3><p>{clients.length ? "Try a different name, phone, or email search." : "Add a client once, then access their Messenger conversation and booking history from one place."}</p><button className="secondary-button" type="button" onClick={() => { setEditing(null); setShowForm(true); }}><Plus aria-hidden="true" /> {clients.length ? "Add client" : "Add first client"}</button></div> : <div className="client-list">{visibleClients.map((client) => <ClientRow client={client} key={client.id} selected={selected?.id === client.id} onSelect={() => setSelected(client)} onAddMessenger={() => { setEditing(client); setShowForm(true); }} />)}</div>}</div>{selected && <ClientDetail client={selected} onClose={() => setSelected(null)} onEdit={() => { setEditing(selected); setShowForm(true); }} />}</section>
     {showForm && <ClientForm initial={editing} clients={clients} onClose={() => { setShowForm(false); setEditing(null); }} onSave={async (client) => { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); const payload = { display_name: client.name, email: client.email || null, facebook_url: client.messenger || null, phone: client.phone || null, address: client.address || null, notes: client.notes || null }; const result = editing ? await database.from("clients").update(payload).eq("id", client.id).select("id, display_name, email, facebook_url, phone, address, notes").single() : await database.from("clients").insert(payload).select("id, display_name, email, facebook_url, phone, address, notes").single(); if (result.error || !result.data) throw new Error(result.error?.message || "Client could not be saved."); const saved = { ...client, id: result.data.id }; setClients((current) => { const next = editing ? current.map((item) => item.id === saved.id ? saved : item) : [...current, saved]; window.localStorage.setItem("salikha-clients", JSON.stringify(next)); return next; }); setSelected(saved); setShowForm(false); setEditing(null); }} />}
  </>;
}

function ClientRow({ client, selected, onSelect, onAddMessenger }: { client: ClientRecord; selected: boolean; onSelect: () => void; onAddMessenger: () => void }) {
  return <div className={`client-row ${selected ? "selected" : ""}`}><button className="client-identity client-select" type="button" onClick={onSelect}><strong>{client.name}</strong><small>{client.phone || client.email || "No contact details"}</small></button><span className="client-stat"><small>Bookings</small><strong>{client.bookingCount ?? 0}</strong></span><span className="client-stat"><small>Balance</small><strong>₱{(client.outstandingBalance ?? 0).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></span><span className="client-action">{client.messenger ? <a href={client.messenger} target="_blank" rel="noreferrer" aria-label={`Open Messenger for ${client.name}`}><ExternalLink aria-hidden="true" /></a> : <button className="missing-link" type="button" onClick={onAddMessenger}>Add link</button>}</span></div>;
}

function ClientDetail({ client, onClose, onEdit }: { client: ClientRecord; onClose: () => void; onEdit: () => void }) {
  const bookings = client.bookingHistory ?? [];
  return <aside className="client-detail panel"><div className="detail-top"><button className="close-button" type="button" onClick={onClose} aria-label="Close client detail"><X aria-hidden="true" /></button><button className="detail-edit" type="button" onClick={onEdit}><Pencil aria-hidden="true" /> Edit</button><button className="detail-edit danger-button" type="button" onClick={() => { if (window.confirm("Delete this client? This cannot be undone.")) void deleteRecord("clients", client.id, "salikha-clients"); }}>Delete</button></div><div className="detail-profile"><p className="eyebrow">Client profile</p><h2>{client.name}</h2><div className="detail-links">{client.messenger && <a href={client.messenger} target="_blank" rel="noreferrer"><ExternalLink aria-hidden="true" /> Open Messenger</a>}</div></div><div className="detail-info"><div><small>Phone</small><strong>{client.phone || "Not provided"}</strong></div><div><small>Email</small><strong>{client.email || "Not provided"}</strong></div><div><small>Address</small><strong>{client.address || "Not provided"}</strong></div></div><div className="detail-section"><p className="eyebrow">Booking history · {client.bookingCount ?? 0}</p>{bookings.length === 0 ? <div className="detail-empty"><CalendarDays aria-hidden="true" /><span>No bookings connected yet.</span></div> : <div className="client-booking-history">{bookings.map((booking) => <div className="client-booking-item" key={booking.id}><span><strong>{booking.eventName}</strong><small>{bookingDateLabel(booking.date)} · {booking.status}</small></span><strong>₱{booking.total.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div>)}</div>}</div><div className="detail-section"><p className="eyebrow">Internal notes</p><p className="detail-notes">{client.notes || "No internal notes."}</p></div></aside>;
}

function ClientForm({ initial, clients, onClose, onSave }: { initial: ClientRecord | null; clients: ClientRecord[]; onClose: () => void; onSave: (client: ClientRecord) => Promise<void> }) {
  const [name, setName] = useState(initial?.name ?? ""); const [messenger, setMessenger] = useState(initial?.messenger ?? ""); const [phone, setPhone] = useState(initial?.phone ?? ""); const [email, setEmail] = useState(initial?.email ?? ""); const [address, setAddress] = useState(initial?.address ?? ""); const [notes, setNotes] = useState(initial?.notes ?? ""); const [saveError, setSaveError] = useState(""); const [saving, setSaving] = useState(false); const duplicate = name.trim().length > 1 && clients.some((client) => client.id !== initial?.id && client.name.toLowerCase() === name.trim().toLowerCase()); const invalidMessenger = Boolean(messenger && !/^https?:\/\//i.test(messenger));
  const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!name.trim() || duplicate || invalidMessenger || saving) return; setSaving(true); setSaveError(""); try { await onSave({ ...(initial ?? { id: crypto.randomUUID(), bookingCount: 0, upcomingBookings: [], outstandingBalance: 0 }), name: name.trim(), messenger: messenger.trim(), phone: phone.trim(), email: email.trim(), address: address.trim(), notes: notes.trim() }); } catch (error) { setSaveError(error instanceof Error ? error.message : "Client could not be saved."); } finally { setSaving(false); } };
  return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="client-form-title"><form className="booking-drawer" onSubmit={submit}><div className="drawer-header"><div><p className="eyebrow">{initial ? "Update record" : "New record"}</p><h2 id="client-form-title">{initial ? "Edit client" : "Add client"}</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close client form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Client information</strong><small>Save the details your team needs most.</small></div></div><div className="form-grid"><label className="wide-field">Full name *<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Juan Dela Cruz" required /></label><label>Phone number<div className="input-with-icon"><Phone aria-hidden="true" /><input value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="09XX XXX XXXX" /></div></label><label>Email address<div className="input-with-icon"><Mail aria-hidden="true" /><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="client@email.com" /></div></label><label className="wide-field">Address<input value={address} onChange={(event) => setAddress(event.target.value)} placeholder="Client address (optional)" /></label></div>{duplicate && <p className="form-warning">A client with this name already exists. Review the existing record before creating another.</p>}</div><div className="form-section"><div className="form-section-title"><span className="step-number">2</span><div><strong>Messenger connection</strong><small>Open the client conversation without leaving the system.</small></div></div><div className="form-grid"><label className="wide-field">Messenger link<div className="input-with-icon"><ExternalLink aria-hidden="true" /><input value={messenger} onChange={(event) => setMessenger(event.target.value)} placeholder="https://m.me/..." /></div></label></div>{invalidMessenger && <p className="form-warning">Use a valid http or https Messenger URL.</p>}</div><div className="form-section"><div className="form-section-title"><span className="step-number">3</span><div><strong>Internal notes</strong><small>Private context for the studio team.</small></div></div><div className="form-grid"><label className="wide-field">Notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Add context for the team..." rows={4} /></label></div></div>{saveError && <p className="form-warning" role="alert">{saveError}</p>}</div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose}>Cancel</button><button className="primary-button" type="submit" disabled={saving || duplicate || invalidMessenger}><Check aria-hidden="true" /> {saving ? "Saving…" : initial ? "Update client" : "Save client"}</button></div></form></div>;
}

function CalendarView() {
  const today = new Date();
  const [viewDate, setViewDate] = useState(new Date(today.getFullYear(), today.getMonth(), 1));
  const [selectedDay, setSelectedDay] = useState(today.getDate());
  const [packageLegend, setPackageLegend] = useState<Array<{ id: string; name: string; color: string }>>([]);
  const [bookings, setBookings] = useState<Array<{ id: string; package_id: string | null; package_name?: string; package_color: string; event_name: string; event_date: string; start_time: string | null; end_time: string | null; venue: string | null; maps_url: string | null; status: string; clients: { display_name: string } | null }>>([]);
  useEffect(() => { let cancelled = false; const database = getLocalDatabase(); if (!database) return; void (async () => { const [bookingResult, packageResult] = await Promise.all([database.from("bookings").select("id, package_id, event_name, event_date, start_time, end_time, venue, maps_url, status, clients(display_name)"), database.from("service_packages").select("id, name, calendar_color")]); if (cancelled || bookingResult.error || packageResult.error || !bookingResult.data || !packageResult.data) return; const packageRows = packageResult.data as Array<{ id: string; name: string; calendar_color: string | null }>; setPackageLegend(packageRows.map((item) => ({ id: item.id, name: item.name, color: item.calendar_color || DEFAULT_PACKAGE_CALENDAR_COLOR }))); const packageById = new Map(packageRows.map((item) => [item.id, item])); setBookings((bookingResult.data as unknown as Array<Omit<(typeof bookings)[number], "package_name" | "package_color">>).map((booking) => { const pack = booking.package_id ? packageById.get(booking.package_id) : undefined; return { ...booking, event_date: bookingDateInput(booking.event_date), package_name: pack?.name || "Custom service", package_color: pack?.calendar_color || DEFAULT_PACKAGE_CALENDAR_COLOR }; })); })(); return () => { cancelled = true; }; }, []);
  const year = viewDate.getFullYear(); const month = viewDate.getMonth(); const monthName = viewDate.toLocaleDateString("en-US", { month: "long", year: "numeric" });
  const firstDay = new Date(year, month, 1).getDay(); const daysInMonth = new Date(year, month + 1, 0).getDate();
  const cells = Array.from({ length: 42 }, (_, index) => { const day = index - firstDay + 1; return day > 0 && day <= daysInMonth ? day : null; });
  const monthKey = `${year}-${String(month + 1).padStart(2, "0")}`;
  const todayKey = manilaDateKey(today);
  const calendarBookings = bookings.filter((booking) => booking.status === "PENDING" && booking.event_date >= todayKey);
  const monthBookings = calendarBookings.filter((booking) => booking.event_date.startsWith(monthKey));
  const bookingsByDay = monthBookings.reduce<Record<number, typeof bookings>>((acc, booking) => { const day = Number(booking.event_date.slice(8, 10)); if (!acc[day]) acc[day] = []; acc[day].push(booking); return acc; }, {});
  const selectedDateBookings = [...(bookingsByDay[selectedDay] || [])].sort((a, b) => (a.start_time || "").localeCompare(b.start_time || ""));
  const goMonth = (offset: number) => { setViewDate(new Date(year, month + offset, 1)); setSelectedDay(1); };
  return <><div className="calendar-crud-actions">{selectedDateBookings.slice(0, 1).map((booking) => <button className="secondary-button danger-button" type="button" key={booking.id} onClick={() => { if (window.confirm("Delete this calendar event and its booking? This cannot be undone.")) void deleteRecord("bookings", booking.id); }}>Delete selected event</button>)}</div>
    <section className="page-heading calendar-heading"><div><p className="eyebrow">Workspace / Calendar</p><h1>Calendar</h1><p className="subheading">See upcoming pending bookings, grouped by package color.</p></div></section>
    <GoogleCalendarControls />
    <section className="calendar-layout"><div className="calendar-panel panel"><div className="calendar-toolbar"><button className="calendar-nav" type="button" onClick={() => goMonth(-1)} aria-label="Previous month"><ChevronLeft aria-hidden="true" /></button><h2>{monthName}</h2><button className="calendar-nav" type="button" onClick={() => goMonth(1)} aria-label="Next month"><ChevronRight aria-hidden="true" /></button><button className="today-button" type="button" onClick={() => { setViewDate(new Date(today.getFullYear(), today.getMonth(), 1)); setSelectedDay(today.getDate()); }}>Today</button></div><div className="calendar-weekdays">{["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-grid">{cells.map((day, index) => <button className={`calendar-day ${day === selectedDay ? "selected" : ""} ${day === today.getDate() && month === today.getMonth() && year === today.getFullYear() ? "today" : ""}`} key={`${year}-${month}-${index}`} type="button" disabled={!day} aria-label={day ? `${monthName} ${day}${bookingsByDay[day] ? `, ${bookingsByDay[day].length} events` : ""}` : undefined} onClick={() => day && setSelectedDay(day)}>{day && <><span>{day}</span>{bookingsByDay[day]?.slice(0, 2).map((booking) => <i className="calendar-event-chip" key={booking.id} style={{ backgroundColor: booking.package_color }} title={`${booking.event_name} · ${booking.package_name}`}>{booking.event_name}</i>)}{(bookingsByDay[day]?.length || 0) > 2 && <small className="calendar-more">+{bookingsByDay[day].length - 2} more</small>}</>}</button>)}</div></div><aside className="agenda-panel panel"><div className="panel-heading"><div><p className="eyebrow">Agenda</p><h2>{monthName}</h2></div><span className="count-badge">{monthBookings.length} events</span></div><div className="agenda-date"><strong>{selectedDay}</strong><span>{new Date(year, month, selectedDay).toLocaleDateString("en-US", { weekday: "long", month: "long" })}</span></div>{selectedDateBookings.length === 0 ? <div className="empty-state calendar-empty"><div className="empty-mark"><CalendarDays aria-hidden="true" /></div><h3>No events scheduled</h3><p>Upcoming pending bookings for this date will appear here.</p></div> : <div className="agenda-list">{selectedDateBookings.map((booking) => <article className="agenda-item" key={booking.id}><div className="agenda-event-title"><strong style={{ backgroundColor: booking.package_color }}>{booking.event_name}</strong><span>{booking.package_name}</span></div><div className="agenda-event-details"><span><small>Date</small><strong>{bookingDateLabel(booking.event_date)}</strong></span><span><small>Time</small><strong>{booking.start_time ? bookingTimeLabel(booking.start_time) : "Not set"} – {booking.end_time ? bookingTimeLabel(booking.end_time) : "Not set"}</strong></span><span><small>Venue</small><strong>{booking.venue || "Not set"}</strong></span></div></article>)}</div>}</aside></section>
    <section className="calendar-legend">{packageLegend.length ? packageLegend.map((item) => <span key={item.id}><i className="legend-swatch" style={{ backgroundColor: item.color }} />{item.name}</span>) : <span className="legend-empty">No packages in catalog</span>}<span className="legend-note">Pending events · Asia/Manila</span></section>
  </>;
}

function CalendarSetup({ onClose }: { onClose: () => void }) {
  return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="calendar-setup-title"><div className="calendar-setup-dialog"><div className="drawer-header"><div><p className="eyebrow">Integration setup</p><h2 id="calendar-setup-title">Connect Google Calendar</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close calendar setup"><X aria-hidden="true" /></button></div><div className="calendar-setup-body"><div className="setup-hero"><div className="setup-icon"><CalendarRange aria-hidden="true" /></div><div><strong>Booking-to-calendar sync</strong><p>When a booking moves to Pending, Salikha Studio OS will create a controlled event in your selected business calendar.</p></div></div><div className="setup-rule-list"><div><Check aria-hidden="true" /><span>Create events on Pending</span></div><div><Check aria-hidden="true" /><span>Update date, time, venue, and details</span></div><div><Check aria-hidden="true" /><span>Remove events on Draft or Cancelled</span></div></div><label className="setup-select-label">Target calendar<select disabled><option>Calendar selection available after OAuth setup</option></select></label><p className="setup-notice"><ArrowLeft aria-hidden="true" /> Google account connection will be enabled in the integration phase. No Calendar ID is stored yet.</p></div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose}>Close</button><button className="primary-button" type="button" disabled>Connect account</button></div></div></div>;
}

type CashTransaction = { id: string; date: string; account: string; type: string; description: string; amount: number; direction: "INFLOW" | "OUTFLOW"; status: "POSTED" | "VOIDED"; reference?: string | null; };
type CashAccount = { id: string; name: string; opening_balance: number; is_active: boolean };
type LocalDbCashAccountRow = { id: string; name: string; opening_balance: number; is_active: boolean };
type LocalDbCashTransactionRow = { id: string; transaction_date: string; type: string; description: string; amount: number; direction: "INFLOW" | "OUTFLOW"; status: "POSTED" | "VOIDED"; reference: string | null; cash_accounts: { name: string } | null };
type CashReconciliation = { id: string; account_id: string; reconciled_date: string; actual_balance: number; system_balance: number; difference: number };

function CashflowView() {
  const [transactions, setTransactions] = useState<CashTransaction[]>([]); const [accounts, setAccounts] = useState<CashAccount[]>([]); const [reconciliations, setReconciliations] = useState<CashReconciliation[]>([]); const [showEntry, setShowEntry] = useState(false); const [showReconciliation, setShowReconciliation] = useState(false); const [query, setQuery] = useState(""); const [typeFilter, setTypeFilter] = useState("All types"); const [directionFilter, setDirectionFilter] = useState("All directions"); const [voidingId, setVoidingId] = useState<string | null>(null); const [justVoidedId, setJustVoidedId] = useState<string | null>(null); const [voidNotice, setVoidNotice] = useState<{ message: string; error: boolean } | null>(null); useEffect(() => { let cancelled = false; const loadCashflow = async () => { const database = getLocalDatabase(); if (!database) return; const [accountRes, transactionRes, reconciliationRes] = await Promise.all([database.from("cash_accounts").select("id, name, opening_balance, is_active").order("name"), database.from("cash_transactions").select("id, transaction_date, type, description, amount, direction, status, reference, cash_accounts(name)").order("transaction_date", { ascending: false }), database.from("cash_reconciliations").select("id, account_id, reconciled_date, actual_balance, system_balance, difference").order("reconciled_date", { ascending: false })]); if (!cancelled) { if (!accountRes.error && accountRes.data) setAccounts((accountRes.data as unknown as LocalDbCashAccountRow[]).map((item) => ({ id: item.id, name: item.name, opening_balance: Number(item.opening_balance), is_active: item.is_active }))); if (!transactionRes.error && transactionRes.data) setTransactions((transactionRes.data as unknown as LocalDbCashTransactionRow[]).map((item) => ({ id: item.id, date: item.transaction_date, account: item.cash_accounts?.name || "Unknown", type: item.type, description: item.description, amount: Number(item.amount), direction: item.direction, status: item.status, reference: item.reference }))); if (!reconciliationRes.error && reconciliationRes.data) setReconciliations((reconciliationRes.data as unknown as CashReconciliation[]).map((row) => ({ ...row, actual_balance: Number(row.actual_balance), system_balance: Number(row.system_balance), difference: Number(row.difference) }))); } }; void loadCashflow(); return () => { cancelled = true; }; }, []);
  useEffect(() => { void fetch("/api/cash-account-rename", { method: "POST" }).then((response) => response.json()).then((result) => { if (result.renamed) window.location.reload(); }).catch(() => undefined); }, []);
  const visibleTransactions = transactions.filter((transaction) => (!query || `${transaction.description} ${transaction.type} ${transaction.account}`.toLowerCase().includes(query.toLowerCase())) && (typeFilter === "All types" || transaction.type === typeFilter) && (directionFilter === "All directions" || transaction.direction === directionFilter));
  const money = (amount: number) => `₱${amount.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`;
  const latestReconciliation = (accountId: string) => reconciliations.find((item) => item.account_id === accountId);
  const accountBalanceAt = (accountId: string, date: string) => { const account = accounts.find((item) => item.id === accountId); if (!account) return 0; const transactionsThroughDate = transactions.filter((item) => item.date <= date && item.status === "POSTED" && item.account === account.name).reduce((total, item) => total + (item.direction === "INFLOW" ? item.amount : -item.amount), 0); return account.opening_balance + transactionsThroughDate; };
  const unreconciledCount = accounts.filter((account) => { const last = latestReconciliation(account.id); return !last || transactions.some((item) => item.account === account.name && item.status === "POSTED" && item.date > last.reconciled_date); }).length;
  const voidTransaction = async (transaction: CashTransaction) => {
    if (transaction.status === "VOIDED" || voidingId) return;
    const confirmed = await showConfirm(`Void this transaction?\n\n${transaction.description}\n${transaction.direction === "INFLOW" ? "+" : "−"}${money(transaction.amount)}\n\nIt will be excluded from verified cash balances. The ledger entry remains for audit history.`, { title: "Void ledger transaction", confirmLabel: "Void transaction", danger: true });
    if (!confirmed) return;
    setVoidingId(transaction.id); setVoidNotice(null);
    try {
      const database = getLocalDatabase();
      const result = await database.from("cash_transactions").update({ status: "VOIDED" }).eq("id", transaction.id).select("id, status").single();
      if (result.error) throw new Error(result.error.message);
      setTransactions((current) => current.map((item) => item.id === transaction.id ? { ...item, status: "VOIDED" } : item));
      setJustVoidedId(transaction.id);
      setVoidNotice({ message: `Voided “${transaction.description}”. Cash totals have been updated; the audit record is retained.`, error: false });
      window.setTimeout(() => setJustVoidedId((current) => current === transaction.id ? null : current), 1100);
      window.setTimeout(() => setVoidNotice((current) => current?.error ? current : null), 6000);
    } catch (error) {
      setVoidNotice({ message: error instanceof Error ? `Could not void this transaction: ${error.message}` : "Could not void this transaction.", error: true });
    } finally { setVoidingId(null); }
  };
  const clearCashflow = async () => { if (!await showConfirm("Clear ALL cash transactions and reconciliations? Cash accounts and opening balances will be kept. This cannot be undone.", { title: "Clear cashflow records", confirmLabel: "Clear records", danger: true })) return; try { const response = await fetch("/api/cashflow-clear", { method: "POST" }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Cashflow records could not be cleared."); setTransactions([]); setReconciliations([]); await showNotice(`Cleared ${result.transactions} transactions and ${result.reconciliations} reconciliations.`, "Cashflow cleared"); } catch (error) { await showNotice(error instanceof Error ? error.message : "Cashflow records could not be cleared.", "Could not clear cashflow"); } };
  return <>
    <section className="page-heading cashflow-heading"><div><p className="eyebrow">Finance / Cashflow</p><h1>Cashflow</h1><p className="subheading">Verified money movement across every business account.</p></div><div className="heading-actions"><button className="secondary-button danger-button" type="button" onClick={() => void clearCashflow()}>Clear records</button><button className="secondary-button" type="button" onClick={() => setShowReconciliation(true)}><ShieldCheck aria-hidden="true" /> Reconcile</button><button className="primary-button" type="button" onClick={() => setShowEntry(true)}><Plus aria-hidden="true" /> Record transaction</button></div></section>
    <section className="finance-access-note"><ShieldCheck aria-hidden="true" /><span>Financial workspace · Visible to Owner, Admin, and Finance roles</span></section>
     <section className="cashflow-metrics"><div className="cashflow-total"><div className="metric-top"><span>Total verified cash</span><CircleDollarSign aria-hidden="true" /></div><strong>{money(accounts.reduce((total, account) => total + Number(account.opening_balance), 0) + transactions.filter((t) => t.status === "POSTED").reduce((total, t) => total + (t.direction === "INFLOW" ? t.amount : -t.amount), 0))}</strong><small>Across {accounts.length} connected accounts</small><div className="cashflow-trend"><TrendingUp aria-hidden="true" /> {transactions.filter((t) => t.status === "POSTED").length} posted entries</div></div><div className="finance-metric"><span>Posted inflows</span><strong>{money(transactions.filter((t) => t.status === "POSTED" && t.direction === "INFLOW").reduce((total, t) => total + t.amount, 0))}</strong><small>All time</small></div><div className="finance-metric"><span>Posted outflows</span><strong>{money(transactions.filter((t) => t.status === "POSTED" && t.direction === "OUTFLOW").reduce((total, t) => total + t.amount, 0))}</strong><small>All time</small></div><div className="finance-metric"><span>Needs reconciliation</span><strong>{unreconciledCount}</strong><small>Accounts need review</small></div></section>
    <section className="account-grid">{accounts.map((account) => { const balance = accountBalanceAt(account.id, "9999-12-31"); const reconciliation = latestReconciliation(account.id); const needsReview = !reconciliation || transactions.some((item) => item.account === account.name && item.status === "POSTED" && item.date > reconciliation.reconciled_date); return <article className="account-card" key={account.id}><div className="account-card-top"><span className="account-icon"><Banknote aria-hidden="true" /></span><span className={`account-state ${needsReview ? "needs-review" : ""}`}><i /> {needsReview ? "Needs review" : "Reconciled"}</span></div><span className="account-name">{account.name}</span><strong>{money(balance)}</strong><small>Book balance</small><div className="account-footer"><span>Last reconciled: {reconciliation ? reconciliation.reconciled_date : "Never"}{reconciliation ? ` · variance ${money(reconciliation.difference)}` : ""}</span></div></article>; })}</section>
     <section className="cashflow-toolbar"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} aria-label="Search transactions" placeholder="Search description or reference" /></div><select className="filter-select" value={typeFilter} onChange={(event) => setTypeFilter(event.target.value)} aria-label="Filter by transaction type"><option>All types</option><option>Other income</option><option>Other expense</option><option>Opening balance</option><option>Transfer between accounts</option></select><select className="filter-select" value={directionFilter} onChange={(event) => setDirectionFilter(event.target.value)} aria-label="Filter by direction"><option>All directions</option><option>INFLOW</option><option>OUTFLOW</option></select><button className="refresh-button" type="button" aria-label="Reset transaction filters" onClick={() => { setQuery(""); setTypeFilter("All types"); setDirectionFilter("All directions"); }}><RefreshCw aria-hidden="true" /></button></section>
     <section className="cashflow-ledger panel"><div className="panel-heading"><div><p className="eyebrow">Verified ledger</p><h2>Transactions</h2></div><span className="muted-label">{visibleTransactions.length} records</span></div>{voidNotice && <div className={`void-notice ${voidNotice.error ? "error" : "success"}`} role={voidNotice.error ? "alert" : "status"}><ShieldCheck aria-hidden="true" /><span>{voidNotice.message}</span><button type="button" aria-label="Dismiss notice" onClick={() => setVoidNotice(null)}><X aria-hidden="true" /></button></div>}<div className="ledger-head"><span>Date</span><span>Account</span><span>Type / description</span><span>Reference</span><span>Amount</span><span>End-of-day balance</span><span /></div>{visibleTransactions.length === 0 ? <div className="booking-empty"><div className="empty-mark"><WalletCards aria-hidden="true" /></div><h3>{transactions.length ? "No matching transactions" : "No cash transactions yet"}</h3><p>{transactions.length ? "Try resetting the transaction filters." : "Booking payments, paid expenses, and manual ledger entries appear here."}</p><button className="secondary-button" type="button" onClick={() => setShowEntry(true)}><Plus aria-hidden="true" /> Record transaction</button></div> : <div className="transaction-list">{visibleTransactions.map((transaction) => <TransactionRow transaction={transaction} bookBalance={accountBalanceAt(accounts.find((account) => account.name === transaction.account)?.id || "", transaction.date)} key={transaction.id} onVoid={voidTransaction} voiding={voidingId === transaction.id} justVoided={justVoidedId === transaction.id} />)}</div>}</section>
     {showEntry && <CashEntryForm accounts={accounts.filter((account) => account.is_active)} onClose={() => setShowEntry(false)} onSave={async (transaction) => { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); const accountId = accounts.find((a) => a.name === transaction.account)?.id; if (!accountId) throw new Error("Select an active cash account."); const result = await database.from("cash_transactions").insert({ account_id: accountId, transaction_date: transaction.date, type: transaction.type, description: transaction.description, amount: transaction.amount, direction: transaction.direction, status: transaction.status }).select("id").single(); if (result.error || !result.data) throw new Error(result.error?.message || "Transaction was not saved."); setTransactions((current) => [{ ...transaction, id: result.data.id }, ...current]); setShowEntry(false); }} />}{showReconciliation && <ReconciliationDialog accounts={accounts.filter((account) => account.is_active)} transactions={transactions} onClose={() => setShowReconciliation(false)} onSave={(record) => { setReconciliations((current) => [record, ...current]); setShowReconciliation(false); }} />}
  </>;
}

function TransactionRow({ transaction, bookBalance, onVoid, voiding, justVoided }: { transaction: CashTransaction; bookBalance: number; onVoid: (transaction: CashTransaction) => void; voiding: boolean; justVoided: boolean }) { const systemPosted = Boolean(transaction.reference); return <div className={`transaction-row ${transaction.status === "VOIDED" ? "voided" : ""} ${justVoided ? "just-voided" : ""}`}><span>{transaction.date}</span><span>{transaction.account}</span><span><strong>{transaction.type}</strong><small>{transaction.description}</small></span><span className="muted-label">{transaction.status === "VOIDED" ? "Voided · excluded from totals" : systemPosted ? "System-posted" : "Manual entry"}</span><span className={transaction.direction === "INFLOW" ? "inflow" : "outflow"}>{transaction.direction === "INFLOW" ? "+" : "−"}₱{transaction.amount.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span><span>₱{bookBalance.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span><span>{!systemPosted && <button className="row-menu" type="button" aria-label={transaction.status === "VOIDED" ? "Transaction already voided" : `Void ${transaction.description}`} disabled={transaction.status === "VOIDED" || voiding} onClick={() => onVoid(transaction)}>{voiding ? "Voiding…" : transaction.status === "VOIDED" ? "Voided" : "Void"}</button>}</span></div>; }

function CashEntryForm({ accounts, onClose, onSave }: { accounts: CashAccount[]; onClose: () => void; onSave: (transaction: CashTransaction) => Promise<void> }) { const [direction, setDirection] = useState<"INFLOW" | "OUTFLOW">("INFLOW"); const [account, setAccount] = useState(accounts[0]?.name || ""); const [type, setType] = useState("Other income"); const [description, setDescription] = useState(""); const [amount, setAmount] = useState(""); const [date, setDate] = useState(manilaDateKey(new Date())); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!account || !description.trim() || !date || !Number.isFinite(Number(amount)) || Number(amount) <= 0) { setError("Enter an account, date, description, and positive amount."); return; } setSaving(true); setError(""); try { await onSave({ id: crypto.randomUUID(), date, account, type, description: description.trim(), amount: Number(amount), direction, status: "POSTED" }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not save transaction."); } finally { setSaving(false); } }; return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="cash-entry-title"><form className="booking-drawer" onSubmit={submit}><div className="drawer-header"><div><p className="eyebrow">Verified ledger</p><h2 id="cash-entry-title">Record transaction</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close transaction form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Transaction details</strong><small>Manual entries are posted immediately and audited.</small></div></div><div className="direction-toggle"><button className={direction === "INFLOW" ? "active" : ""} type="button" onClick={() => { setDirection("INFLOW"); setType("Other income"); }}><TrendingUp aria-hidden="true" /> Inflow</button><button className={direction === "OUTFLOW" ? "active" : ""} type="button" onClick={() => { setDirection("OUTFLOW"); setType("Other expense"); }}><TrendingDown aria-hidden="true" /> Outflow</button></div><div className="form-grid"><label>Account<select value={account} onChange={(event) => setAccount(event.target.value)} required>{accounts.map((item) => <option key={item.id} value={item.name}>{item.name}</option>)}</select></label><label>Type<select value={type} onChange={(event) => setType(event.target.value)}><option>{direction === "INFLOW" ? "Other income" : "Other expense"}</option><option>Opening balance</option></select></label><label className="wide-field">Description<input value={description} onChange={(event) => setDescription(event.target.value)} placeholder="What is this transaction for?" required /></label><label>Amount<div className="money-input"><span>₱</span><input value={amount} onChange={(event) => setAmount(event.target.value)} inputMode="decimal" placeholder="0.00" required /></div></label><label>Date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} required /></label></div></div><div className="entry-warning"><ShieldCheck aria-hidden="true" /><span>This will affect the verified account balance and cannot be edited after posting. Voiding keeps the audit trail.</span></div>{error && <p className="form-warning" role="alert">{error}</p>}</div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose}>Cancel</button><button className="primary-button" type="submit" disabled={saving || accounts.length === 0}><Check aria-hidden="true" /> {saving ? "Posting…" : "Post transaction"}</button></div></form></div>; }

function ReconciliationDialog({ accounts, transactions, onClose, onSave }: { accounts: CashAccount[]; transactions: CashTransaction[]; onClose: () => void; onSave: (record: CashReconciliation) => void }) { const [accountId, setAccountId] = useState(accounts[0]?.id || ""); const [date, setDate] = useState(manilaDateKey(new Date())); const [actual, setActual] = useState(""); const [notes, setNotes] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const account = accounts.find((item) => item.id === accountId); const systemBalance = account ? account.opening_balance + transactions.filter((item) => item.account === account.name && item.date <= date && item.status === "POSTED").reduce((sum, item) => sum + (item.direction === "INFLOW" ? item.amount : -item.amount), 0) : 0; const variance = Number.isFinite(Number(actual)) ? Math.round((Number(actual) - systemBalance) * 100) / 100 : null; const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!accountId || !date || !Number.isFinite(Number(actual)) || Number(actual) < 0) { setError("Enter a valid actual balance and reconciliation date."); return; } setSaving(true); setError(""); try { const response = await fetch("/api/cash-reconciliation", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ accountId, date, actualBalance: Number(actual), notes }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Reconciliation failed."); onSave({ id: result.reconciliation.id, account_id: accountId, reconciled_date: result.reconciliation.reconciled_date, actual_balance: Number(result.reconciliation.actual_balance), system_balance: Number(result.reconciliation.system_balance), difference: Number(result.reconciliation.difference) }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Reconciliation failed."); } finally { setSaving(false); } }; return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="reconciliation-title"><form className="calendar-setup-dialog reconciliation-dialog" onSubmit={submit}><div className="drawer-header"><div><p className="eyebrow">Controls / Finance</p><h2 id="reconciliation-title">Reconcile account</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close reconciliation"><X aria-hidden="true" /></button></div><div className="calendar-setup-body"><div className="setup-hero"><div className="setup-icon"><ShieldCheck aria-hidden="true" /></div><div><strong>Compare actual and book balance</strong><p>Save a dated snapshot. Any variance remains visible; reconciliation will not silently change transactions.</p></div></div><label className="setup-select-label">Account<select value={accountId} onChange={(event) => setAccountId(event.target.value)} required>{accounts.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="setup-select-label reconciliation-input">Reconciliation date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} required /></label><label className="setup-select-label reconciliation-input">Actual balance<div className="money-input"><span>₱</span><input value={actual} onChange={(event) => setActual(event.target.value)} placeholder="0.00" inputMode="decimal" required /></div></label><div className="reconciliation-result"><span>Book balance as of {date}</span><strong>₱{systemBalance.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong><small>{variance === null ? "Enter actual balance to see the variance." : `Variance: ${variance < 0 ? "−" : "+"}₱${Math.abs(variance).toLocaleString("en-PH", { minimumFractionDigits: 2 })}`}</small></div><label className="setup-select-label">Notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} rows={2} /></label>{error && <p className="form-warning" role="alert">{error}</p>}</div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose}>Cancel</button><button className="primary-button" type="submit" disabled={saving || !accounts.length}><Check aria-hidden="true" /> {saving ? "Saving…" : "Save reconciliation"}</button></div></form></div>; }

type ExpenseStatus = "PENDING" | "PAID";
type Expense = { id: string; description: string; amount: number; date: string; category: string; classification: "DIRECT" | "OPERATING"; booking: string; bookingId?: string; account: string; status: ExpenseStatus; };
type LocalDbExpenseRow = { id: string; description: string; amount: number; expense_date: string; category: string; classification: "DIRECT" | "OPERATING"; account: string; status: ExpenseStatus; bookings: { event_name: string } | null; };

function ExpensesView() {
  const [expenses, setExpenses] = useState<Expense[]>([]); const [showForm, setShowForm] = useState(false); const [selected, setSelected] = useState<Expense | null>(null); const [query, setQuery] = useState(""); const [statusFilter, setStatusFilter] = useState("All statuses"); const [categoryFilter, setCategoryFilter] = useState("All categories"); const [classificationFilter, setClassificationFilter] = useState("All classifications");
  useEffect(() => { let cancelled = false; const loadExpenses = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("expenses").select("id, description, amount, expense_date, category, classification, account, status, bookings(event_name)").order("expense_date", { ascending: false }); if (!error && data && !cancelled) { setExpenses((data as unknown as LocalDbExpenseRow[]).map((expense) => ({ id: expense.id, description: expense.description, amount: Number(expense.amount), date: expense.expense_date, category: expense.category, classification: expense.classification, booking: expense.bookings?.event_name || "", account: expense.account, status: expense.status }))); return; } } const stored = window.localStorage.getItem("salikha-expenses"); if (stored && !cancelled) setExpenses(JSON.parse(stored) as Expense[]); }; void loadExpenses(); return () => { cancelled = true; }; }, []);
  const transition = async (expense: Expense, status: ExpenseStatus) => { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); if (status === "PAID") { const response = await fetch("/api/expense-payment", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ expenseId: expense.id }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Expense payment was not posted."); } else { const result = await database.from("expenses").update({ status }).eq("id", expense.id); if (result.error) throw new Error(result.error.message); } const updated = { ...expense, status }; setExpenses((current) => current.map((item) => item.id === expense.id ? updated : item)); setSelected(updated); };
  const removeExpense = async (expense: Expense) => { const response = await fetch("/api/expense-delete", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ expenseId: expense.id }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Expense could not be deleted."); setExpenses((current) => current.filter((item) => item.id !== expense.id)); setSelected(null); };
  const visibleExpenses = expenses.filter((expense) => (!query || `${expense.description} ${expense.category} ${expense.booking}`.toLowerCase().includes(query.toLowerCase())) && (statusFilter === "All statuses" || expense.status === statusFilter) && (categoryFilter === "All categories" || expense.category === categoryFilter) && (classificationFilter === "All classifications" || expense.classification === classificationFilter));
  const currentMonth = manilaDateKey(new Date()).slice(0, 7); const paidTotal = expenses.filter((expense) => expense.status === "PAID" && expense.date.startsWith(currentMonth)).reduce((total, expense) => total + expense.amount, 0); const directTotal = expenses.filter((expense) => expense.classification === "DIRECT" && expense.status === "PAID").reduce((total, expense) => total + expense.amount, 0); const operatingTotal = expenses.filter((expense) => expense.classification === "OPERATING" && expense.status === "PAID").reduce((total, expense) => total + expense.amount, 0); const money = (amount: number) => `₱${amount.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`;
  return <>
    <section className="page-heading"><div><p className="eyebrow">Finance / Expenses</p><h1>Expenses</h1><p className="subheading">Capture business costs clearly before they affect verified cashflow.</p></div><button className="primary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add expense</button></section>
     <section className="expense-metrics"><div className="expense-metric"><span>Paid this month</span><strong>{money(paidTotal)}</strong><small>Posted to Cashflow</small></div><div className="expense-metric"><span>Direct costs</span><strong>{money(directTotal)}</strong><small>Linked to bookings</small></div><div className="expense-metric"><span>Operating costs</span><strong>{money(operatingTotal)}</strong><small>General business costs</small></div></section>
     <section className="booking-toolbar"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search description, category, or booking" aria-label="Search expenses" /></div><select className="filter-select" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)} aria-label="Filter expenses by status"><option>All statuses</option><option>PENDING</option><option>PAID</option></select><select className="filter-select" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)} aria-label="Filter expenses by category"><option>All categories</option><option>Transportation</option><option>Food & beverage</option><option>Supplies</option><option>Utilities</option><option>Other</option></select><select className="filter-select" value={classificationFilter} onChange={(event) => setClassificationFilter(event.target.value)} aria-label="Filter expenses by classification"><option>All classifications</option><option>DIRECT</option><option>OPERATING</option></select></section>
     <section className="expense-layout"><div className="cashflow-ledger panel expense-register"><div className="panel-heading"><div><p className="eyebrow">Expense register</p><h2>All expenses</h2></div><span className="muted-label">{visibleExpenses.length} records</span></div><div className="expense-head"><span>Description</span><span>Date</span><span>Category</span><span>Booking</span><span>Amount</span><span>Account</span><span>Status</span></div>{visibleExpenses.length === 0 ? <div className="booking-empty"><div className="empty-mark"><Receipt aria-hidden="true" /></div><h3>{expenses.length ? "No matching expenses" : "No expenses yet"}</h3><p>{expenses.length ? "Try changing the filters or search term." : "Pending expenses stay out of Cashflow until they are marked Paid."}</p><button className="secondary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add expense</button></div> : <div className="expense-list">{visibleExpenses.map((expense) => <button className={`expense-row ${selected?.id === expense.id ? "selected" : ""}`} type="button" key={expense.id} onClick={() => setSelected(expense)}><span><strong>{expense.description}</strong><small>{expense.classification}</small></span><span>{expense.date}</span><span>{expense.category}</span><span>{expense.booking || "General"}</span><span>{money(expense.amount)}</span><span>{expense.account}</span><span className={`status-pill ${expense.status.toLowerCase()}`}>{expense.status}</span></button>)}</div>}</div>{selected && <ExpenseDetail expense={selected} onClose={() => setSelected(null)} onTransition={transition} onDelete={removeExpense} />}</section>
    {showForm && <ExpenseForm onClose={() => setShowForm(false)} onSave={async (expense) => { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); const result = await database.from("expenses").insert({ booking_id: expense.bookingId || null, description: expense.description, amount: expense.amount, expense_date: expense.date, category: expense.category, classification: expense.classification, account: expense.account, status: expense.status }).select("id").single(); if (result.error || !result.data) throw new Error(result.error?.message || "Expense was not saved."); expense = { ...expense, id: result.data.id }; setExpenses((current) => { const next = [expense, ...current]; window.localStorage.setItem("salikha-expenses", JSON.stringify(next)); return next; }); setSelected(expense); setShowForm(false); }} />}
  </>;
}

function ExpenseDetail({ expense, onClose, onTransition, onDelete }: { expense: Expense; onClose: () => void; onTransition: (expense: Expense, status: ExpenseStatus) => Promise<void>; onDelete: (expense: Expense) => Promise<void> }) { const next = expense.status === "PENDING" ? "PAID" : null; return <aside className="expense-detail panel"><div className="detail-top"><button className="close-button" type="button" onClick={onClose} aria-label="Close expense detail"><X aria-hidden="true" /></button><span className={`status-pill ${expense.status.toLowerCase()}`}>{expense.status}</span></div><div className="expense-detail-title"><p className="eyebrow">Expense detail</p><h2>{expense.description}</h2><strong>₱{expense.amount.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div><div className="expense-info-grid"><div><small>Date</small><strong>{expense.date}</strong></div><div><small>Category</small><strong>{expense.category}</strong></div><div><small>Classification</small><strong>{expense.classification}</strong></div><div><small>Account</small><strong>{expense.account}</strong></div></div><div className="expense-detail-section"><p className="eyebrow">Payment status</p><div className="expense-timeline"><span className={expense.status === "PENDING" ? "current" : "done"}><Clock3 aria-hidden="true" /> Pending</span><span className={expense.status === "PAID" ? "current" : ""}><WalletCards aria-hidden="true" /> Paid</span></div></div>{next && <button className="primary-button expense-next-action" type="button" onClick={async () => { try { await onTransition(expense, next); } catch (cause) { window.alert(cause instanceof Error ? cause.message : "Could not post expense payment."); } }}>Mark as paid <ArrowRight aria-hidden="true" /></button>}<button className="secondary-button danger-button" type="button" onClick={async () => { if (!window.confirm(expense.status === "PAID" ? "Delete this paid expense? Its linked cash outflow will be voided and kept in the ledger." : "Delete this expense? This cannot be undone.")) return; try { await onDelete(expense); } catch (cause) { window.alert(cause instanceof Error ? cause.message : "Expense could not be deleted."); } }}>Delete expense</button></aside>; }

function ExpenseForm({ onClose, onSave }: { onClose: () => void; onSave: (expense: Expense) => Promise<void> }) {
  const [classification, setClassification] = useState<"DIRECT" | "OPERATING">("DIRECT"); const [description, setDescription] = useState(""); const [amount, setAmount] = useState(""); const [category, setCategory] = useState("Transportation"); const [bookingId, setBookingId] = useState(""); const [expenseDate, setExpenseDate] = useState(manilaDateKey(new Date())); const [account, setAccount] = useState(""); const [bookings, setBookings] = useState<Array<{ id: string; event_name: string; event_date: string; clients: { display_name: string } | null }>>([]); const [accounts, setAccounts] = useState<string[]>([]); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  useEffect(() => { const database = getLocalDatabase(); if (!database) return; void Promise.all([database.from("bookings").select("id, event_name, event_date, clients(display_name)").order("event_date"), database.from("cash_accounts").select("id, name, is_active").order("name")]).then(([bookingResult, accountResult]) => { if (bookingResult.error) setError(bookingResult.error.message); else setBookings((bookingResult.data || []) as unknown as typeof bookings); if (accountResult.error) setError(accountResult.error.message); else { const names = ((accountResult.data || []) as Array<{ name: string; is_active: boolean }>).filter((row) => row.is_active).map((row) => row.name); setAccounts(names); setAccount(names[0] || ""); } }); }, []);
  const selectedBooking = bookings.find((row) => row.id === bookingId); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!description.trim() || !expenseDate || !account || Number(amount) <= 0 || (classification === "DIRECT" && !selectedBooking)) { setError("Complete the expense details, select an active account, and link direct costs to a booking."); return; } setSaving(true); setError(""); try { await onSave({ id: crypto.randomUUID(), description: description.trim(), amount: Number(amount), date: expenseDate, category, booking: selectedBooking?.event_name || "", bookingId: selectedBooking?.id, account, classification, status: "PENDING" }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not save expense."); } finally { setSaving(false); } };
  return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="expense-form-title"><form className="booking-drawer" onSubmit={submit}><div className="drawer-header"><div><p className="eyebrow">New record</p><h2 id="expense-form-title">Add expense</h2></div><button className="close-button" type="button" onClick={onClose} aria-label="Close expense form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Expense information</strong><small>New expenses remain pending until paid.</small></div></div><div className="form-grid"><label className="wide-field">Description<input value={description} onChange={(event) => setDescription(event.target.value)} placeholder="e.g. Transport to event venue" required /></label><label>Amount<div className="money-input"><span>₱</span><input value={amount} onChange={(event) => setAmount(event.target.value)} inputMode="decimal" placeholder="0.00" required /></div></label><label>Expense date<input type="date" value={expenseDate} onChange={(event) => setExpenseDate(event.target.value)} required /></label><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}><option>Transportation</option><option>Meals</option><option>Supplies</option><option>Equipment</option><option>Other</option></select></label><label>Payment account<select value={account} onChange={(event) => setAccount(event.target.value)} required>{accounts.map((name) => <option key={name}>{name}</option>)}</select></label></div></div><div className="form-section"><div className="form-section-title"><span className="step-number">2</span><div><strong>Cost classification</strong><small>Direct costs must be connected to a booking.</small></div></div><div className="classification-toggle"><button type="button" className={classification === "DIRECT" ? "active" : ""} onClick={() => setClassification("DIRECT")}>Direct cost</button><button type="button" className={classification === "OPERATING" ? "active" : ""} onClick={() => setClassification("OPERATING")}>Operating cost</button></div><label className="wide-field">Booking {classification === "DIRECT" ? "*" : "(optional)"}<select value={bookingId} onChange={(event) => setBookingId(event.target.value)} required={classification === "DIRECT"}><option value="">{classification === "DIRECT" ? "Select a real booking" : "No booking / general cost"}</option>{bookings.map((row) => <option value={row.id} key={row.id}>{row.event_name} · {row.clients?.display_name || "Client"} · {bookingDateLabel(row.event_date)}</option>)}</select>{classification === "DIRECT" && bookings.length === 0 && <small>No bookings available; add a booking first or choose operating cost.</small>}</label></div>{error && <p className="form-warning" role="alert">{error}</p>}</div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose}>Cancel</button><button className="primary-button" type="submit" disabled={saving || !accounts.length}><Check aria-hidden="true" /> {saving ? "Saving…" : "Save as pending"}</button></div></form></div>;
}

type InventoryItem = { id: string; name: string; category: string; unit: string; pcsPerUnit: number; sellingPrice: number; unitCost: number; stock: number; reorderLevel: number; supplier: string; };
type LocalDbInventoryRow = { id: string; name: string; category: string; unit: string; pcs_per_unit: number; selling_price: number; average_unit_cost: number; on_hand: number; reorder_level: number; supplier: string; };

function InventoryView({ onNavigate }: { onNavigate: (view: string) => void }) {
  const [items, setItems] = useState<InventoryItem[]>([]); const [showForm, setShowForm] = useState(false); const [editingItem, setEditingItem] = useState<InventoryItem | null>(null); const [selected, setSelected] = useState<InventoryItem | null>(null); const [query, setQuery] = useState(""); const [categoryFilter, setCategoryFilter] = useState("All categories"); const [stockFilter, setStockFilter] = useState("All stock"); const lowStock = items.filter((item) => item.stock <= item.reorderLevel).length; const money = (amount: number) => `₱${amount.toLocaleString("en-PH", { minimumFractionDigits: 2 })}`;
  useEffect(() => { let cancelled = false; const loadItems = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("inventory_items").select("id, name, category, unit, pcs_per_unit, selling_price, average_unit_cost, on_hand, reorder_level, supplier").eq("is_active", true).order("name"); if (!error && data && !cancelled) { setItems((data as unknown as LocalDbInventoryRow[]).map((item) => ({ id: item.id, name: item.name, category: item.category, unit: item.unit, pcsPerUnit: Number(item.pcs_per_unit), sellingPrice: Number(item.selling_price), unitCost: Number(item.average_unit_cost), stock: Number(item.on_hand), reorderLevel: Number(item.reorder_level), supplier: item.supplier || "" }))); return; } } const stored = window.localStorage.getItem("salikha-inventory"); if (stored && !cancelled) setItems((JSON.parse(stored) as Array<InventoryItem & { pcsPerUnit?: number }>).map((item) => ({ ...item, pcsPerUnit: item.pcsPerUnit || 1 }))); }; void loadItems(); return () => { cancelled = true; }; }, []);
  const visibleItems = items.filter((item) => (!query || `${item.name} ${item.category} ${item.supplier}`.toLowerCase().includes(query.toLowerCase())) && (categoryFilter === "All categories" || item.category === categoryFilter) && (stockFilter === "All stock" || (stockFilter === "Low stock" ? item.stock <= item.reorderLevel : item.stock > item.reorderLevel)));
  const updateStock = async (item: InventoryItem, delta: number, movementType: "STOCK_IN" | "USAGE" | "RETURN" = delta > 0 ? "STOCK_IN" : "USAGE") => { if (!delta) return; try { const response = await fetch("/api/inventory-stock", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ itemId: item.id, quantity: Math.abs(delta), type: movementType, notes: movementType === "RETURN" ? "Returned from booking" : movementType === "STOCK_IN" ? "Manual stock received" : "Manual stock usage" }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Inventory movement failed."); const updated = { ...item, stock: Number(result.onHand) }; setItems((current) => { const next = current.map((entry) => entry.id === item.id ? updated : entry); window.localStorage.setItem("salikha-inventory", JSON.stringify(next)); return next; }); setSelected(updated); } catch (error) { window.alert(error instanceof Error ? error.message : "Inventory movement failed."); } };
  return <>
    <section className="page-heading"><div><p className="eyebrow">Operations / Inventory</p><h1>Inventory</h1><p className="subheading">Track consumables, price lists, and event usage in one place.</p></div><div className="heading-actions">{selected && <button className="secondary-button" type="button" onClick={() => { setEditingItem(selected); setShowForm(true); }}><Pencil aria-hidden="true" /> Edit selected</button>}<button className="primary-button" type="button" onClick={() => { setEditingItem(null); setShowForm(true); }}><Plus aria-hidden="true" /> Add consumable</button></div></section>
     <section className="inventory-metrics"><div className="inventory-metric"><span>Total items</span><strong>{items.length}</strong><small>Active consumables</small></div><div className="inventory-metric"><span>Stock value</span><strong>{money(items.reduce((total, item) => total + item.stock * item.unitCost, 0))}</strong><small>Weighted average cost</small></div><div className="inventory-metric"><span>Low stock</span><strong>{lowStock}</strong><small>At or below reorder level</small></div><div className="inventory-metric inventory-price-metric"><span>Event price list</span><strong>{money(items.reduce((total, item) => total + item.sellingPrice, 0))}</strong><small>Internal charge reference</small></div></section>
     <section className="inventory-notice"><Boxes aria-hidden="true" /><div><strong>Automatic event deduction is ready</strong><span>Package recipes define expected usage. Actual quantities are deducted when an event is reconciled.</span></div><button className="secondary-button" type="button" onClick={() => onNavigate("Packages")}>Manage package recipes <ArrowRight aria-hidden="true" /></button></section>
     <section className="booking-toolbar"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search consumables or suppliers" aria-label="Search inventory" /></div><select className="filter-select" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)} aria-label="Filter inventory by category"><option>All categories</option><option>Paper</option><option>Ink</option><option>Packaging</option><option>Supplies</option><option>Other</option></select><select className="filter-select" value={stockFilter} onChange={(event) => setStockFilter(event.target.value)} aria-label="Filter inventory by stock status"><option>All stock</option><option>Low stock</option><option>Healthy stock</option></select></section>
    <section className="inventory-layout"><div className="inventory-register panel"><div className="panel-heading"><div><p className="eyebrow">Consumables</p><h2>Inventory price list</h2></div><span className="muted-label">{visibleItems.length} items</span></div><div className="inventory-head"><span>Item</span><span>Category / unit</span><span>Current stock</span><span>Stock value</span><span>Status</span><span /></div>{visibleItems.length === 0 ? <div className="booking-empty"><div className="empty-mark"><Boxes aria-hidden="true" /></div><h3>{items.length ? "No matching consumables" : "No consumables yet"}</h3><p>{items.length ? "Try changing the search or filters." : "Add your supplies and their costs once. Stock movements will keep the balance updated after that."}</p><button className="secondary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add consumable</button></div> : <div className="inventory-list">{visibleItems.map((item) => <button className={`inventory-row ${selected?.id === item.id ? "selected" : ""}`} type="button" key={item.id} onClick={() => setSelected(item)}><span><strong>{item.name}</strong><small>{money(item.sellingPrice)} internal price</small></span><span>{item.category} / {item.unit}</span><span>{item.stock} {item.unit}</span><span>{money(item.stock * item.unitCost)}</span><span className={`stock-pill ${item.stock <= item.reorderLevel ? "low" : "healthy"}`}>{item.stock <= item.reorderLevel ? "Low stock" : "Healthy"}</span><span className="row-menu">•••</span></button>)}</div>}</div>{selected && <InventoryDetail item={selected} onClose={() => setSelected(null)} onStockChange={updateStock} />}</section>
    {showForm && <InventoryForm initial={editingItem} onClose={() => { setShowForm(false); setEditingItem(null); }} onSave={async (item) => { try { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); if (editingItem) { const result = await database.from("inventory_items").update({ name: item.name, category: item.category, unit: item.unit, pcs_per_unit: item.pcsPerUnit, selling_price: item.sellingPrice, average_unit_cost: item.unitCost, reorder_level: item.reorderLevel, supplier: item.supplier }).eq("id", editingItem.id); if (result.error) throw new Error(result.error.message); setItems((current) => current.map((entry) => entry.id === editingItem.id ? item : entry)); setSelected(item); } else { const response = await fetch("/api/inventory-create", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: item.name, category: item.category, unit: item.unit, pcsPerUnit: item.pcsPerUnit, unitCost: item.unitCost, sellingPrice: item.sellingPrice, openingStock: item.stock, reorderLevel: item.reorderLevel, supplier: item.supplier }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Inventory item was not saved."); const row = result.item; item = { ...item, id: row.id, stock: Number(row.on_hand) }; setItems((current) => [item, ...current]); setSelected(item); } setShowForm(false); setEditingItem(null); } catch (error) { window.alert(error instanceof Error ? error.message : "Inventory item was not saved."); } }} />}
  </>;
}

function InventoryDetail({ item, onClose, onStockChange }: { item: InventoryItem; onClose: () => void; onStockChange: (item: InventoryItem, delta: number, type?: "STOCK_IN" | "USAGE" | "RETURN") => void }) { const [movements, setMovements] = useState<Array<{ id: string; movement_type: string; quantity: number; unit_cost: number; notes: string | null; created_at: string }>>([]); useEffect(() => { let cancelled = false; const database = getLocalDatabase(); if (!database) return; database.from("inventory_movements").select("id, movement_type, quantity, unit_cost, notes, created_at").eq("inventory_item_id", item.id).order("created_at", { ascending: false }).then((result) => { if (!cancelled && !result.error && result.data) setMovements((result.data as unknown as typeof movements).map((movement) => ({ ...movement, quantity: Number(movement.quantity), unit_cost: Number(movement.unit_cost) }))); }); return () => { cancelled = true; }; }, [item.id, item.stock]); const removeItem = async () => { if (!window.confirm("Remove this inventory item? Items with history or recipe links will be archived instead of deleted.")) return; try { const response = await fetch("/api/inventory-delete", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ inventoryItemId: item.id }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Inventory item could not be removed."); window.alert(result.action === "archived" ? "This item has linked history or recipes, so it was archived instead of deleted." : "Inventory item deleted."); window.location.reload(); } catch (error) { window.alert(error instanceof Error ? error.message : "Inventory item could not be removed."); } }; return <aside className="inventory-detail panel"><div className="detail-top"><button className="close-button" type="button" onClick={onClose} aria-label="Close inventory detail"><X aria-hidden="true" /></button><span className="muted-label">Local database</span></div><div className="inventory-detail-title"><p className="eyebrow">Consumable detail</p><h2>{item.name}</h2><span>{item.category} · {item.unit}</span></div><div className="stock-highlight"><div><small>Current stock</small><strong>{item.stock} <em>{item.unit}</em></strong></div><span className={`stock-pill ${item.stock <= item.reorderLevel ? "low" : "healthy"}`}>{item.stock <= item.reorderLevel ? "Low stock" : "Healthy"}</span></div><div className="inventory-info"><div><small>Price / {item.unit}</small><strong>₱{item.unitCost.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div><div><small>Cost / piece ({item.pcsPerUnit})</small><strong>₱{(item.unitCost / item.pcsPerUnit).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div><div><small>Internal price</small><strong>₱{item.sellingPrice.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div><div><small>Reorder level</small><strong>{item.reorderLevel} {item.unit}</strong></div><div><small>Supplier</small><strong>{item.supplier || "Not provided"}</strong></div></div><div className="inventory-actions"><button className="secondary-button" type="button" onClick={() => onStockChange(item, 1, "STOCK_IN")}><Plus aria-hidden="true" /> Stock in</button><button className="secondary-button" type="button" disabled={item.stock === 0} onClick={() => onStockChange(item, -1, "USAGE")}><Minus aria-hidden="true" /> Usage</button><button className="secondary-button" type="button" onClick={() => onStockChange(item, 1, "RETURN")}><RotateCcw aria-hidden="true" /> Return</button></div><div className="detail-section"><p className="eyebrow">Movement history</p>{movements.length ? <div className="maintenance-list">{movements.map((movement) => <div key={movement.id} className="maintenance-row"><strong>{movement.movement_type.replace("_", " ")} · {movement.quantity} {item.unit}</strong><small>{new Date(movement.created_at).toLocaleString("en-PH", { timeZone: "Asia/Manila" })}</small>{movement.notes && <small>{movement.notes}</small>}<span>₱{(movement.quantity * movement.unit_cost).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span></div>)}</div> : <div className="detail-empty"><History aria-hidden="true" /><span>No stock movements have been recorded.</span></div>}</div><button className="secondary-button danger-button" type="button" onClick={() => void removeItem()}>Remove item</button></aside>; }

function InventoryForm({ initial, onClose, onSave }: { initial: InventoryItem | null; onClose: () => void; onSave: (item: InventoryItem) => void | Promise<void> }) { const [name, setName] = useState(initial?.name ?? ""); const [category, setCategory] = useState(initial?.category ?? "Paper"); const [unit, setUnit] = useState(initial?.unit ? initial.unit[0].toUpperCase() + initial.unit.slice(1) : "Piece"); const [pcsPerUnit, setPcsPerUnit] = useState(String(initial?.pcsPerUnit ?? 1)); const [sellingPrice, setSellingPrice] = useState(String(initial?.sellingPrice ?? "")); const [unitCost, setUnitCost] = useState(String(initial?.unitCost ?? "")); const [stock, setStock] = useState(String(initial?.stock ?? "")); const [supplier, setSupplier] = useState(initial?.supplier ?? ""); const [saving, setSaving] = useState(false); const pieceCost = (Number(unitCost) || 0) / Math.max(Number(pcsPerUnit) || 1, 1); return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="inventory-form-title"><form className="booking-drawer" onSubmit={async (event) => { event.preventDefault(); if (saving || !name.trim() || Number(pcsPerUnit) <= 0 || Number(unitCost) < 0 || (!initial && Number(stock) < 0)) return; setSaving(true); try { await onSave({ ...(initial ?? { id: crypto.randomUUID() }), name: name.trim(), category, unit: unit.toLowerCase(), pcsPerUnit: Number(pcsPerUnit), sellingPrice: Number(sellingPrice) || 0, unitCost: Number(unitCost) || 0, stock: initial ? initial.stock : Number(stock) || 0, reorderLevel: initial?.reorderLevel ?? 0, supplier: supplier.trim() }); } finally { setSaving(false); } }}><div className="drawer-header"><div><p className="eyebrow">{initial ? "Update consumable" : "New consumable"}</p><h2 id="inventory-form-title">{initial ? "Edit inventory item" : "Add inventory item"}</h2></div><button className="close-button" type="button" onClick={onClose} disabled={saving} aria-label="Close inventory form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Item information</strong><small>{initial ? "Current stock is preserved; change it with stock actions." : "Stock quantity uses the selected unit."}</small></div></div><div className="form-grid"><label className="wide-field">Item name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. 4R Paper" required /></label><label>Category<select value={category} onChange={(event) => setCategory(event.target.value)}><option>Paper</option><option>Ink</option><option>Packaging</option><option>Supplies</option><option>Other</option></select></label><label>Unit<select value={unit} onChange={(event) => setUnit(event.target.value)}><option>Piece</option><option>Pack</option><option>Box</option></select></label><label>Price per {unit.toLowerCase()}<div className="money-input"><span>₱</span><input value={unitCost} onChange={(event) => setUnitCost(event.target.value)} type="number" min="0" step="0.01" placeholder="0.00" required /></div></label><label>Pieces per {unit.toLowerCase()}<input value={pcsPerUnit} onChange={(event) => setPcsPerUnit(event.target.value)} type="number" min="0.001" step="0.001" required /></label>{initial ? <label>Current stock<input value={stock} readOnly /></label> : <label>Opening stock ({unit.toLowerCase()}s)<input value={stock} onChange={(event) => setStock(event.target.value)} type="number" min="0" step="0.001" placeholder="0" required /></label>}<label>Internal price (optional)<div className="money-input"><span>₱</span><input value={sellingPrice} onChange={(event) => setSellingPrice(event.target.value)} type="number" min="0" step="0.01" placeholder="0.00" /></div></label><label>Supplier<input value={supplier} onChange={(event) => setSupplier(event.target.value)} placeholder="Supplier" /></label></div><p className="subheading">Cost per piece: ₱{pieceCost.toFixed(2)} (₱{Number(unitCost || 0).toFixed(2)} ÷ {Number(pcsPerUnit || 1)}).</p></div></div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose} disabled={saving}>Cancel</button><button className="primary-button" type="submit" disabled={saving}><Check aria-hidden="true" /> {saving ? "Saving…" : initial ? "Save changes" : "Save item"}</button></div></form></div>; }

type EquipmentStatus = "GREAT" | "NEEDS_ATTENTION" | "FOR_FIXING" | "BROKEN";
type EquipmentRecord = { id: string; name: string; category: string; purchaseDate: string; purchaseCost: number; status: EquipmentStatus; nextMaintenance: string; lastCleaned: string; };
type LocalDbEquipmentRow = { id: string; name: string; status: string; condition: string; notes: string | null; purchase_date: string | null; purchase_cost: number; next_maintenance_date: string | null; last_cleaned_date: string | null; };
type MaintenanceRecord = { id: string; scheduled_date: string; completed_date: string | null; action_type: string; cost: number; vendor: string; notes: string | null };
type LocalDbMaintenanceRow = { id: string; scheduled_date: string; completed_date: string | null; action_type: string; cost: number; vendor: string; notes: string | null };

function EquipmentView() {
  const [equipment, setEquipment] = useState<EquipmentRecord[]>([]); const [showForm, setShowForm] = useState(false); const [selected, setSelected] = useState<EquipmentRecord | null>(null); const [query, setQuery] = useState(""); const [statusFilter, setStatusFilter] = useState("All statuses"); const [maintenanceFilter, setMaintenanceFilter] = useState("All maintenance"); const today = new Date(); const reminderDate = new Date(today.getTime() + 14 * 86400000); const dueSoon = equipment.filter((item) => item.nextMaintenance && new Date(item.nextMaintenance) <= reminderDate).length;
  useEffect(() => { let cancelled = false; const loadEquipment = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("equipment").select("id, name, status, condition, notes, purchase_date, purchase_cost, next_maintenance_date, last_cleaned_date").order("name"); if (!error && data && !cancelled) { setEquipment((data as unknown as LocalDbEquipmentRow[]).map((item) => ({ id: item.id, name: item.name, category: item.condition || "Uncategorized", purchaseDate: item.purchase_date || "", purchaseCost: Number(item.purchase_cost || 0), status: (item.status as EquipmentStatus) || "GREAT", nextMaintenance: item.next_maintenance_date || "", lastCleaned: item.last_cleaned_date || "" }))); return; } } if (!cancelled) setEquipment([]); }; void loadEquipment(); return () => { cancelled = true; }; }, []);
  const visibleEquipment = equipment.filter((item) => (!query || `${item.name} ${item.category}`.toLowerCase().includes(query.toLowerCase())) && (statusFilter === "All statuses" || item.status === statusFilter) && (maintenanceFilter === "All maintenance" || (maintenanceFilter === "Due soon" ? item.nextMaintenance !== "" && new Date(item.nextMaintenance) <= reminderDate : maintenanceFilter === "Scheduled later" ? new Date(item.nextMaintenance).getTime() > reminderDate.getTime() : !item.nextMaintenance)));
  return <>
    <section className="page-heading"><div><p className="eyebrow">Operations / Equipment</p><h1>Equipment</h1><p className="subheading">Know what is ready, what needs care, and what must stay off an event.</p></div><div className="heading-actions"><button className="secondary-button danger-button" type="button" disabled={!selected} onClick={() => { if (selected && window.confirm("Delete this equipment? This cannot be undone.")) void deleteRecord("equipment", selected.id, "salikha-equipment"); }}>Delete selected</button><button className="primary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add equipment</button></div></section>
     {dueSoon > 0 && <section className="maintenance-alert"><AlertCircle aria-hidden="true" /><div><strong>{dueSoon} equipment {dueSoon === 1 ? "needs" : "need"} maintenance soon</strong><span>Maintenance reminders appear 14 days before the scheduled date.</span></div><button className="secondary-button" type="button" onClick={() => setMaintenanceFilter("Due soon")}>View maintenance queue <ArrowRight aria-hidden="true" /></button></section>}
    <section className="equipment-metrics"><div className="equipment-metric"><span>Total equipment</span><strong>{equipment.length}</strong><small>Registered assets</small></div><div className="equipment-metric"><span>Great</span><strong>{equipment.filter((item) => item.status === "GREAT").length}</strong><small>Ready for assignment</small></div><div className="equipment-metric warning"><span>Needs attention</span><strong>{equipment.filter((item) => item.status === "NEEDS_ATTENTION").length}</strong><small>Inspect before use</small></div><div className="equipment-metric danger"><span>For fixing / broken</span><strong>{equipment.filter((item) => item.status === "FOR_FIXING" || item.status === "BROKEN").length}</strong><small>Assignment blocked</small></div></section>
     <section className="booking-toolbar"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search equipment or category" aria-label="Search equipment" /></div><select className="filter-select" value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)} aria-label="Filter equipment by status"><option>All statuses</option><option value="GREAT">Great</option><option value="NEEDS_ATTENTION">Needs Attention</option><option value="FOR_FIXING">For Fixing</option><option value="BROKEN">Broken</option></select><select className="filter-select" value={maintenanceFilter} onChange={(event) => setMaintenanceFilter(event.target.value)} aria-label="Filter equipment by maintenance"><option>All maintenance</option><option>Due soon</option><option>Scheduled later</option></select></section>
    <section className="equipment-layout"><div className="equipment-register panel"><div className="panel-heading"><div><p className="eyebrow">Asset register</p><h2>All equipment</h2></div><span className="muted-label">{visibleEquipment.length} assets</span></div><div className="equipment-head"><span>Equipment</span><span>Status</span><span>Next maintenance</span><span>Last cleaned</span><span>Quick action</span></div>{visibleEquipment.length === 0 ? <div className="booking-empty"><div className="empty-mark"><Camera aria-hidden="true" /></div><h3>{equipment.length ? "No matching equipment" : "No equipment registered"}</h3><p>{equipment.length ? "Try changing the search or filters." : "Add your cameras, printers, booths, and other reusable assets to start tracking their care."}</p><button className="secondary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add equipment</button></div> : <div className="equipment-list">{visibleEquipment.map((item) => <button className={`equipment-row ${selected?.id === item.id ? "selected" : ""}`} type="button" key={item.id} onClick={() => setSelected(item)}><span><strong>{item.name}</strong><small>{item.category}</small></span><span className={`equipment-status ${item.status.toLowerCase()}`}>{item.status.replace("_", " ")}</span><span>{maintenanceLabel(item.nextMaintenance)}</span><span>{item.lastCleaned || "Not recorded"}</span><span className="quick-action">View details <ArrowRight aria-hidden="true" /></span></button>)}</div>}</div>{selected && <EquipmentDetail equipment={selected} onClose={() => setSelected(null)} onSave={async (updated) => { try { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); const result = await database.from("equipment").update({ name: updated.name, status: updated.status, condition: updated.category, purchase_date: updated.purchaseDate || null, purchase_cost: updated.purchaseCost, next_maintenance_date: updated.nextMaintenance || null, last_cleaned_date: updated.lastCleaned || null }).eq("id", updated.id); if (result.error) throw new Error(result.error.message); setEquipment((current) => current.map((item) => item.id === updated.id ? updated : item)); setSelected(updated); } catch (error) { window.alert(error instanceof Error ? error.message : "Equipment was not updated."); } }} />}</section>
    <section className="equipment-footer-note"><CalendarDays aria-hidden="true" /><span>Maintenance reminders will also appear on the Dashboard and internal Calendar.</span></section>
    {showForm && <EquipmentForm onClose={() => setShowForm(false)} onSave={async (item) => { try { const database = getLocalDatabase(); if (!database) throw new Error("Local database is unavailable."); const result = await database.from("equipment").insert({ name: item.name, status: item.status, condition: item.category, purchase_date: item.purchaseDate || null, purchase_cost: item.purchaseCost, next_maintenance_date: item.nextMaintenance || null, last_cleaned_date: item.lastCleaned || null }).select("id").single(); if (result.error || !result.data) throw new Error(result.error?.message || "Equipment was not saved."); item = { ...item, id: result.data.id }; setEquipment((current) => [item, ...current]); setSelected(item); setShowForm(false); } catch (error) { window.alert(error instanceof Error ? error.message : "Equipment was not saved."); } }} />}
  </>;
}

function maintenanceLabel(date: string) { if (!date) return "Not scheduled"; const days = Math.ceil((new Date(date).getTime() - Date.now()) / 86400000); if (days < 0) return "Overdue"; if (days <= 14) return `${days} days left`; return new Date(date).toLocaleDateString("en-PH", { month: "short", day: "numeric", year: "numeric" }); }

function EquipmentDetail({ equipment, onClose, onSave }: { equipment: EquipmentRecord; onClose: () => void; onSave: (equipment: EquipmentRecord) => void }) { const [status, setStatus] = useState(equipment.status); const [maintenance, setMaintenance] = useState<MaintenanceRecord[]>([]); const [showMaintenance, setShowMaintenance] = useState(false); useEffect(() => { let cancelled = false; const database = getLocalDatabase(); if (!database) return; database.from("equipment_maintenance").select("id, scheduled_date, completed_date, action_type, cost, vendor, notes").eq("equipment_id", equipment.id).order("scheduled_date", { ascending: false }).then((result) => { if (!cancelled && !result.error && result.data) setMaintenance(result.data as unknown as LocalDbMaintenanceRow[]); }); return () => { cancelled = true; }; }, [equipment.id]); const maintenanceTotal = maintenance.reduce((sum, item) => sum + Number(item.cost || 0), 0); return <aside className="equipment-detail panel"><div className="detail-top"><button className="close-button" type="button" onClick={onClose} aria-label="Close equipment detail"><X aria-hidden="true" /></button><span className={`equipment-status ${status.toLowerCase()}`}>{status.replace("_", " ")}</span></div><div className="equipment-detail-title"><p className="eyebrow">Equipment detail</p><h2>{equipment.name}</h2><span>{equipment.category}</span></div><div className="equipment-info"><div><small>Purchase date</small><strong>{equipment.purchaseDate || "Not provided"}</strong></div><div><small>Purchase cost</small><strong>₱{equipment.purchaseCost.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong></div><div><small>Next maintenance</small><strong>{equipment.nextMaintenance || "Not scheduled"}</strong></div><div><small>Last cleaned</small><strong>{equipment.lastCleaned || "Not recorded"}</strong></div></div><div className="detail-section"><p className="eyebrow">Equipment status</p><select className="status-select" value={status} onChange={(event) => setStatus(event.target.value as EquipmentStatus)}><option value="GREAT">Great</option><option value="NEEDS_ATTENTION">Needs Attention</option><option value="FOR_FIXING">For Fixing</option><option value="BROKEN">Broken</option></select>{status === "BROKEN" && <p className="form-warning">Broken equipment cannot be assigned to a booking.</p>}<button className="primary-button detail-save" type="button" onClick={() => onSave({ ...equipment, status })}><Check aria-hidden="true" /> Save status</button></div><div className="detail-section"><p className="eyebrow">Maintenance timeline</p>{maintenance.length === 0 ? <div className="maintenance-empty"><Wrench aria-hidden="true" /><span>No maintenance records yet.</span></div> : <div className="maintenance-list">{maintenance.map((item) => <div key={item.id} className="maintenance-row"><strong>{item.action_type}</strong><small>{item.scheduled_date}{item.completed_date ? ` · completed ${item.completed_date}` : ""}</small>{item.vendor && <small>{item.vendor}</small>}{item.notes && <small>{item.notes}</small>}<span>₱{Number(item.cost).toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span></div>)}</div>}<button className="secondary-button maintenance-action" type="button" onClick={() => setShowMaintenance(true)}><Plus aria-hidden="true" /> Add maintenance action</button></div><div className="detail-section"><p className="eyebrow">Maintenance costs</p>{maintenance.length === 0 ? <div className="maintenance-empty"><CircleDollarSign aria-hidden="true" /><span>No costs recorded.</span></div> : <div className="maintenance-cost-row"><strong>Total recorded cost</strong><span>₱{maintenanceTotal.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span></div>}</div>{showMaintenance && <MaintenanceActionForm equipmentId={equipment.id} equipmentName={equipment.name} onClose={() => setShowMaintenance(false)} onSave={async (record) => { setMaintenance((current) => [record, ...current]); setShowMaintenance(false); }} />}</aside>; }

function EquipmentForm({ onClose, onSave }: { onClose: () => void; onSave: (equipment: EquipmentRecord) => void | Promise<void> }) { const [name, setName] = useState(""); const [category, setCategory] = useState("Camera"); const [purchaseDate, setPurchaseDate] = useState(""); const [purchaseCost, setPurchaseCost] = useState(""); const [nextMaintenance, setNextMaintenance] = useState(""); const [saving, setSaving] = useState(false); return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="equipment-form-title"><form className="booking-drawer" onSubmit={async (event) => { event.preventDefault(); if (saving || !name.trim()) return; setSaving(true); try { await onSave({ id: crypto.randomUUID(), name: name.trim(), category, purchaseDate, purchaseCost: Number(purchaseCost) || 0, status: "GREAT", nextMaintenance, lastCleaned: "" }); } finally { setSaving(false); } }}><div className="drawer-header"><div><p className="eyebrow">New asset</p><h2 id="equipment-form-title">Add equipment</h2></div><button className="close-button" type="button" onClick={onClose} disabled={saving} aria-label="Close equipment form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Equipment information</strong><small>Register the asset and its maintenance schedule.</small></div></div><div className="form-grid"><label className="wide-field">Equipment name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. DNP photo printer" required /></label><label>Category/type<select value={category} onChange={(event) => setCategory(event.target.value)}><option>Camera</option><option>Printer</option><option>Photobooth</option><option>Lighting</option><option>Computer</option><option>Other</option></select></label><label>Purchase date<input type="date" value={purchaseDate} onChange={(event) => setPurchaseDate(event.target.value)} /></label><label>Purchase cost<div className="money-input"><span>₱</span><input value={purchaseCost} onChange={(event) => setPurchaseCost(event.target.value)} inputMode="decimal" placeholder="0.00" /></div></label><label>Next maintenance date<input type="date" value={nextMaintenance} onChange={(event) => setNextMaintenance(event.target.value)} /></label></div></div><div className="inventory-form-note"><AlertTriangle aria-hidden="true" /><span>A reminder will appear 14 days before the scheduled maintenance date. Status is manually updated after inspection.</span></div></div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose} disabled={saving}>Cancel</button><button className="primary-button" type="submit" disabled={saving}><Check aria-hidden="true" /> {saving ? "Saving…" : "Save equipment"}</button></div></form></div>; }

type PackageRecord = { id: string; name: string; description: string; basePrice: number; duration: string; inclusions: string; notes: string; calendarColor?: string; active: boolean; };
type LocalDbPackageRow = { id: string; name: string; description: string | null; duration: string | null; inclusions: string | null; notes: string | null; calendar_color: string | null; base_price: number; is_active: boolean; };

function PackagesView({ onNavigate, onCreateBooking }: { onNavigate: (view: string) => void; onCreateBooking: () => void }) {
  const [packages, setPackages] = useState<PackageRecord[]>([]); const [recipeSummary, setRecipeSummary] = useState<Record<string, { ingredientCount: number; recipeCost: number }>>({}); const [showForm, setShowForm] = useState(false); const [editingPackage, setEditingPackage] = useState<PackageRecord | null>(null); const [selected, setSelected] = useState<PackageRecord | null>(null); const [query, setQuery] = useState(""); const [activeFilter, setActiveFilter] = useState("All statuses"); const [inclusionFilter, setInclusionFilter] = useState("All inclusions"); useEffect(() => { let cancelled = false; const loadPackages = async () => { const database = getLocalDatabase(); if (database) { const { data, error } = await database.from("service_packages").select("id, name, description, duration, inclusions, notes, calendar_color, base_price, is_active").order("name"); if (!error && data && !cancelled) { setPackages((data as LocalDbPackageRow[]).map((item) => ({ id: item.id, name: item.name, description: item.description || "", duration: item.duration || "", inclusions: item.inclusions || "", notes: item.notes || "", calendarColor: item.calendar_color || DEFAULT_PACKAGE_CALENDAR_COLOR, basePrice: Number(item.base_price), active: item.is_active }))); return; } } const stored = window.localStorage.getItem("salikha-packages"); if (stored && !cancelled) setPackages((JSON.parse(stored) as PackageRecord[]).map((item) => ({ ...item, calendarColor: item.calendarColor || DEFAULT_PACKAGE_CALENDAR_COLOR }))); }; void loadPackages(); return () => { cancelled = true; }; }, []);
  useEffect(() => { let cancelled = false; const loadRecipeSummary = async () => { try { const response = await fetch("/api/package-recipe-summary", { cache: "no-store" }); const result = await response.json(); if (!response.ok) throw new Error(result.error); if (!cancelled) setRecipeSummary(Object.fromEntries((result.data as Array<{ packageId: string; ingredientCount: number; recipeCost: string }>).map((row) => [row.packageId, { ingredientCount: row.ingredientCount, recipeCost: Number(row.recipeCost) }]))); } catch { if (!cancelled) setRecipeSummary({}); } }; const refresh = () => void loadRecipeSummary(); void loadRecipeSummary(); window.addEventListener("package-recipe-updated", refresh); return () => { cancelled = true; window.removeEventListener("package-recipe-updated", refresh); }; }, []);
  const activeCount = packages.filter((item) => item.active).length;
  const visiblePackages = packages.filter((item) => (!query || `${item.name} ${item.description}`.toLowerCase().includes(query.toLowerCase())) && (activeFilter === "All statuses" || (activeFilter === "Active" ? item.active : !item.active)) && (inclusionFilter === "All inclusions" || (inclusionFilter === "With inclusions" ? Boolean(item.inclusions.trim()) : !item.inclusions.trim())));
  return <>
    <section className="page-heading"><div><p className="eyebrow">Catalog / Packages</p><h1>Packages</h1><p className="subheading">Build consistent offers with clear pricing and event-ready inclusions.</p></div><button className="primary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add package</button></section>
     <section className="package-metrics"><div className="package-metric"><span>Total packages</span><strong>{packages.length}</strong><small>Catalog records</small></div><div className="package-metric"><span>Active packages</span><strong>{activeCount}</strong><small>Available for new bookings</small></div><div className="package-metric"><span>Packages with inclusions</span><strong>{packages.filter((item) => item.inclusions.trim()).length}</strong><small>Configured service details</small></div><div className="package-metric package-price-metric"><span>Catalog status</span><strong>{packages.length ? "Ready" : "Setup"}</strong><small>Internal price list</small></div></section>
     <section className="booking-toolbar"><div className="search-field"><Search aria-hidden="true" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search package name or description" aria-label="Search packages" /></div><select className="filter-select" value={activeFilter} onChange={(event) => setActiveFilter(event.target.value)} aria-label="Filter packages by status"><option>All statuses</option><option>Active</option><option>Inactive</option></select><select className="filter-select" value={inclusionFilter} onChange={(event) => setInclusionFilter(event.target.value)} aria-label="Filter packages by inclusions"><option>All inclusions</option><option>With inclusions</option><option>Without inclusions</option></select></section>
     <section className="package-layout"><div className="package-register panel"><div className="panel-heading"><div><p className="eyebrow">Service catalog</p><h2>Package list</h2></div><span className="muted-label">{visiblePackages.length} packages</span></div><div className="package-head"><span>Package</span><span>Base price</span><span>Inclusions</span><span>Recipe</span><span>Est. profit</span><span>Status</span><span /></div>{visiblePackages.length === 0 ? <div className="booking-empty"><div className="empty-mark"><ListPlus aria-hidden="true" /></div><h3>{packages.length ? "No matching packages" : "No packages yet"}</h3><p>{packages.length ? "Try changing the search or filters." : "Add your service offers once, then reuse them in bookings with editable price overrides."}</p><button className="secondary-button" type="button" onClick={() => setShowForm(true)}><Plus aria-hidden="true" /> Add package</button></div> : <div className="package-list">{visiblePackages.map((item) => { const recipe = recipeSummary[item.id]; const recipeCost = recipe?.recipeCost || 0; const estimatedProfit = item.basePrice - recipeCost; return <button className={`package-row ${selected?.id === item.id ? "selected" : ""}`} type="button" key={item.id} onClick={() => setSelected(item)}><span className="package-name-cell"><i style={{ backgroundColor: item.calendarColor || DEFAULT_PACKAGE_CALENDAR_COLOR }} /><strong>{item.name}</strong><small>{item.description || "No description"}</small></span><span>₱{item.basePrice.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span><span>{item.inclusions ? item.inclusions.split(",").length : 0} included</span><span className="recipe-state">{recipe?.ingredientCount ? `${recipe.ingredientCount} ingredient${recipe.ingredientCount === 1 ? "" : "s"}` : "Not configured"}</span><span>₱{estimatedProfit.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</span><span className={`status-pill ${item.active ? "paid" : "draft"}`}>{item.active ? "Active" : "Inactive"}</span><span className="row-menu">•••</span></button>; })}</div>}</div>{selected && <PackageDetailActions packageItem={selected} onClose={() => setSelected(null)} onNavigate={onNavigate} onCreateBooking={onCreateBooking} onDuplicate={async (copy) => { const database = getLocalDatabase(); if (database) { const result = await database.from("service_packages").insert({ name: copy.name, description: copy.description || null, duration: copy.duration || null, inclusions: copy.inclusions || null, notes: copy.notes || null, calendar_color: copy.calendarColor || DEFAULT_PACKAGE_CALENDAR_COLOR, base_price: copy.basePrice, is_active: copy.active }).select("id, name, description, duration, inclusions, notes, calendar_color, base_price, is_active").single(); if (!result.error && result.data) copy = { ...copy, id: result.data.id }; } setPackages((current) => { const next = [copy, ...current]; window.localStorage.setItem("salikha-packages", JSON.stringify(next)); return next; }); }} onSave={async (updated) => { const database = getLocalDatabase(); if (database) { await database.from("service_packages").update({ name: updated.name, description: updated.description || null, duration: updated.duration || null, inclusions: updated.inclusions || null, notes: updated.notes || null, calendar_color: updated.calendarColor || DEFAULT_PACKAGE_CALENDAR_COLOR, base_price: updated.basePrice, is_active: updated.active }).eq("id", updated.id); } setPackages((current) => { const next = current.map((item) => item.id === updated.id ? updated : item); window.localStorage.setItem("salikha-packages", JSON.stringify(next)); return next; }); setSelected(updated); }} />}</section>
     {showForm && <PackageForm onClose={() => setShowForm(false)} onSave={async (item) => { const database = getLocalDatabase(); if (database) { const result = await database.from("service_packages").insert({ name: item.name, description: item.description || null, duration: item.duration || null, inclusions: item.inclusions || null, notes: item.notes || null, calendar_color: item.calendarColor || DEFAULT_PACKAGE_CALENDAR_COLOR, base_price: item.basePrice, is_active: item.active }).select("id, name, description, duration, inclusions, notes, calendar_color, base_price, is_active").single(); if (!result.error && result.data) item = { id: result.data.id, name: result.data.name, description: result.data.description || "", duration: result.data.duration || "", inclusions: result.data.inclusions || "", notes: result.data.notes || "", calendarColor: result.data.calendar_color || DEFAULT_PACKAGE_CALENDAR_COLOR, basePrice: Number(result.data.base_price), active: result.data.is_active }; else if (result.error) throw new Error(result.error.message); } setPackages((current) => { const next = [item, ...current]; window.localStorage.setItem("salikha-packages", JSON.stringify(next)); return next; }); setSelected(item); setShowForm(false); }} />}
  </>;
}

function PackageDetail({ packageItem, onClose, onSave }: { packageItem: PackageRecord; onClose: () => void; onSave: (item: PackageRecord) => void }) { const [active, setActive] = useState(packageItem.active); return <aside className="package-detail panel"><div className="detail-top"><button className="close-button" type="button" onClick={onClose} aria-label="Close package detail"><X aria-hidden="true" /></button><button className="detail-edit" type="button"><Pencil aria-hidden="true" /> Edit</button></div><div className="package-detail-title"><p className="eyebrow">Package detail</p><h2>{packageItem.name}</h2><p>{packageItem.description || "No description added."}</p></div><div className="package-price"><small>Base price</small><strong>₱{packageItem.basePrice.toLocaleString("en-PH", { minimumFractionDigits: 2 })}</strong><span>Booking price can be overridden per event.</span></div><div className="package-detail-info"><div><small>Duration</small><strong>{packageItem.duration || "Not specified"}</strong></div><div><small>Status</small><strong>{active ? "Active" : "Inactive"}</strong></div></div><div className="package-detail-section"><p className="eyebrow">Included services</p><p className="package-copy">{packageItem.inclusions || "No inclusions added yet."}</p></div><div className="package-detail-section"><div className="package-section-heading"><p className="eyebrow">Consumable recipe</p><button className="text-button" type="button"><Plus aria-hidden="true" /> Manage recipe</button></div><div className="package-empty-line"><Boxes aria-hidden="true" /><span>No consumables configured.</span></div></div><div className="package-detail-section"><div className="package-section-heading"><p className="eyebrow">Add-ons</p><button className="text-button" type="button"><Plus aria-hidden="true" /> Manage add-ons</button></div><div className="package-empty-line"><ListPlus aria-hidden="true" /><span>No add-ons configured.</span></div></div><div className="package-detail-actions"><button className="secondary-button" type="button"><Copy aria-hidden="true" /> Duplicate</button><button className="secondary-button" type="button" onClick={() => onSave({ ...packageItem, active: !active })}><Archive aria-hidden="true" /> {active ? "Archive" : "Activate"}</button><button className="primary-button" type="button"><Plus aria-hidden="true" /> Create booking</button></div></aside>; }

function PackageForm({ onClose, onSave }: { onClose: () => void; onSave: (item: PackageRecord) => void | Promise<void> }) { const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [price, setPrice] = useState(""); const [duration, setDuration] = useState(""); const [inclusions, setInclusions] = useState(""); const [notes, setNotes] = useState(""); const [calendarColor, setCalendarColor] = useState<string>(DEFAULT_PACKAGE_CALENDAR_COLOR); const [saving, setSaving] = useState(false); return <div className="booking-overlay" role="dialog" aria-modal="true" aria-labelledby="package-form-title"><form className="booking-drawer" onSubmit={async (event) => { event.preventDefault(); if (saving || !name.trim()) return; setSaving(true); try { await onSave({ id: crypto.randomUUID(), name: name.trim(), description: description.trim(), basePrice: Number(price) || 0, duration: duration.trim(), inclusions: inclusions.trim(), notes: notes.trim(), calendarColor, active: true }); } finally { setSaving(false); } }}><div className="drawer-header"><div><p className="eyebrow">New catalog item</p><h2 id="package-form-title">Add package</h2></div><button className="close-button" type="button" onClick={onClose} disabled={saving} aria-label="Close package form"><X aria-hidden="true" /></button></div><div className="drawer-body"><div className="form-section"><div className="form-section-title"><span className="step-number">1</span><div><strong>Package details</strong><small>Define the offer your team can reuse.</small></div></div><div className="form-grid"><label className="wide-field">Package name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Photo Booth Basic" required /></label><label className="wide-field">Description<input value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Short package description" /></label><label>Base price<div className="money-input"><span>₱</span><input value={price} onChange={(event) => setPrice(event.target.value)} inputMode="decimal" placeholder="0.00" /></div></label><label>Estimated duration<input value={duration} onChange={(event) => setDuration(event.target.value)} placeholder="e.g. 3 hours" /></label><fieldset className="wide-field package-color-field"><legend>Calendar color</legend><div className="package-color-options">{PACKAGE_CALENDAR_COLORS.map((color) => <label key={color.name} title={color.name}><input type="radio" name="package-calendar-color" value={color.value} checked={calendarColor === color.value} onChange={() => setCalendarColor(color.value)} /><span style={{ backgroundColor: color.value }} />{color.name}</label>)}</div></fieldset><label className="wide-field">Included services<textarea value={inclusions} onChange={(event) => setInclusions(event.target.value)} placeholder="Separate inclusions with commas" rows={3} /></label><label className="wide-field">Internal notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Private package notes" rows={3} /></label></div></div><div className="inventory-form-note"><Boxes aria-hidden="true" /><span>Consumable recipes and add-ons can be configured after saving, once inventory items are available.</span></div></div><div className="drawer-footer"><button className="secondary-button" type="button" onClick={onClose} disabled={saving}>Cancel</button><button className="primary-button" type="submit" disabled={saving}><Check aria-hidden="true" /> {saving ? "Saving…" : "Save package"}</button></div></form></div>; }

const reportCatalog = ["Revenue report", "Cashflow report", "Outstanding balances", "Booking performance", "Package performance", "Expense report", "Profitability report", "Inventory valuation", "Inventory movement", "Equipment report", "Maintenance report"];
type ReportPoint = { label: string; value: number };
type ReportChartKind = "line" | "pie" | "bar";
function ReportAnalyticsChart({ title, points, kind, format = "money" }: { title: string; points: ReportPoint[]; kind: ReportChartKind; format?: "money" | "count" | "percent" }) {
  const max = Math.max(1, ...points.map((point) => Math.abs(point.value)));
  const display = (value: number) => format === "money" ? `₱${Math.abs(value).toLocaleString("en-PH", { maximumFractionDigits: 0 })}` : format === "percent" ? `${value.toFixed(0)}%` : String(value);
  const palette = ["#d93645", "#4576b8", "#2f936e", "#d49a24", "#8660b5", "#238f9b", "#dd7040", "#7c8798"];
  const pieTotal = points.reduce((sum, point) => sum + Math.abs(point.value), 0);
  let cursor = 0;
  const pieStops = points.map((point, index) => { const start = cursor; cursor += pieTotal ? Math.abs(point.value) / pieTotal * 100 : 0; return `${palette[index % palette.length]} ${start}% ${cursor}%`; }).join(", ");
  const minValue = Math.min(0, ...points.map((point) => point.value));
  const maxValue = Math.max(0, ...points.map((point) => point.value));
  const valueSpan = maxValue - minValue || 1;
  const linePoints = points.map((point, index) => ({ ...point, x: points.length < 2 ? 300 : 24 + index * (552 / (points.length - 1)), y: 20 + (maxValue - point.value) / valueSpan * 68 }));
  const linePath = linePoints.map((point, index) => `${index ? "L" : "M"} ${point.x} ${point.y}`).join(" ");
  return <section className="report-chart analytics-chart"><div className="chart-header"><div><p className="eyebrow">Analytics</p><h3>{title}</h3></div><span>{points.length} groups</span></div>{points.length ? <div className="analytics-visualization">
    {kind === "bar" && <div className="analytics-bars" role="img" aria-label={`${title} bar chart`}>{points.map((point) => <div className="analytics-bar-column" key={point.label} title={`${point.label}: ${display(point.value)}`}><strong>{display(point.value)}</strong><div className={`analytics-bar ${point.value < 0 ? "negative" : ""}`} style={{ height: `${Math.max(3, Math.abs(point.value) / max * 100)}%` }} /><span>{point.label}</span></div>)}</div>}
    {kind === "line" && <svg className="analytics-line" viewBox="0 0 600 120" role="img" aria-label={`${title} line chart`}><path className="analytics-line-guide" d="M24 20H576 M24 54H576 M24 88H576" /><path className="analytics-line-path" d={linePath} />{linePoints.map((point) => <g key={point.label}><title>{`${point.label}: ${display(point.value)}`}</title><circle className="analytics-line-dot" cx={point.x} cy={point.y} r="4" /><text x={point.x} y="111" textAnchor="middle">{point.label}</text></g>)}</svg>}
    {kind === "pie" && <div className="analytics-pie-layout"><div className="analytics-pie" style={{ background: `conic-gradient(${pieStops})` }} role="img" aria-label={`${title} pie chart`}><span>{points.length} groups</span></div><div className="analytics-legend">{points.map((point, index) => <div key={point.label}><i style={{ background: palette[index % palette.length] }} /><span title={point.label}>{point.label}</span><strong>{display(point.value)}</strong></div>)}</div></div>}
  </div> : <div className="analytics-empty">No chartable records in this date range.</div>}</section>;
}
function ReportsView() {
  const [selected, setSelected] = useState(reportCatalog[0]);
  const [period, setPeriod] = useState("This month");
  const [basis, setBasis] = useState("Booking event date");
  const [dateFrom, setDateFrom] = useState(() => `${manilaDateKey(new Date()).slice(0, 7)}-01`);
  const [dateTo, setDateTo] = useState(() => manilaDateKey(new Date()));
  const [generatedAt, setGeneratedAt] = useState("Not generated");
  const [reportError, setReportError] = useState("");
  const [reportData, setReportData] = useState<{ total: number; records: number; label: string }>({ total: 0, records: 0, label: "0 records" });
  const [trendPoints, setTrendPoints] = useState<ReportPoint[]>([]);
  const [breakdownPoints, setBreakdownPoints] = useState<ReportPoint[]>([]);
  const reportRange = (() => { const today = new Date(); const year = today.getFullYear(); const month = today.getMonth(); if (period === "Custom date range") return { from: dateFrom, to: dateTo }; if (period === "Last month") return { from: manilaDateKey(new Date(year, month - 1, 1)), to: manilaDateKey(new Date(year, month, 0)) }; if (period === "This quarter") { const quarterMonth = Math.floor(month / 3) * 3; return { from: manilaDateKey(new Date(year, quarterMonth, 1)), to: manilaDateKey(new Date(year, quarterMonth + 3, 0)) }; } if (period === "This year") return { from: `${year}-01-01`, to: `${year}-12-31` }; return { from: `${year}-${String(month + 1).padStart(2, "0")}-01`, to: manilaDateKey(today) }; })();
  const compositionTotal = breakdownPoints.reduce((sum, point) => sum + Math.abs(point.value), 0);
  const compositionShares = breakdownPoints.map((point) => ({ label: point.label, value: compositionTotal ? Math.abs(point.value) / compositionTotal * 100 : 0 }));
  const reportChartPlan: Record<string, { primaryTitle: string; primaryKind: ReportChartKind; primaryPoints: ReportPoint[]; secondaryTitle: string; secondaryKind: ReportChartKind; secondaryPoints: ReportPoint[] }> = {
    "Revenue report": { primaryTitle: "Revenue trend", primaryKind: "line", primaryPoints: trendPoints, secondaryTitle: "Revenue by source", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Cashflow report": { primaryTitle: "Net cash movement", primaryKind: "line", primaryPoints: trendPoints, secondaryTitle: "Cash in vs. cash out", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Outstanding balances": { primaryTitle: "Outstanding by event date", primaryKind: "bar", primaryPoints: trendPoints, secondaryTitle: "Balance by booking status", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Booking performance": { primaryTitle: "Booking value over time", primaryKind: "line", primaryPoints: trendPoints, secondaryTitle: "Bookings by status", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Package performance": { primaryTitle: "Revenue share by package", primaryKind: "pie", primaryPoints: breakdownPoints, secondaryTitle: "Package revenue trend", secondaryKind: "line", secondaryPoints: trendPoints },
    "Expense report": { primaryTitle: "Paid expenses over time", primaryKind: "bar", primaryPoints: trendPoints, secondaryTitle: "Paid expenses by category", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Profitability report": { primaryTitle: "Net operating result", primaryKind: "line", primaryPoints: trendPoints, secondaryTitle: "Income vs. operating costs", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Inventory valuation": { primaryTitle: "Inventory value by item", primaryKind: "bar", primaryPoints: breakdownPoints, secondaryTitle: "Share among top items", secondaryKind: "pie", secondaryPoints: compositionShares },
    "Inventory movement": { primaryTitle: "Units moved over time", primaryKind: "bar", primaryPoints: trendPoints, secondaryTitle: "Movement by type", secondaryKind: "pie", secondaryPoints: breakdownPoints },
    "Equipment report": { primaryTitle: "Equipment by status", primaryKind: "pie", primaryPoints: breakdownPoints, secondaryTitle: "Status counts", secondaryKind: "bar", secondaryPoints: breakdownPoints },
    "Maintenance report": { primaryTitle: "Scheduled maintenance cost", primaryKind: "line", primaryPoints: trendPoints, secondaryTitle: "Cost by equipment", secondaryKind: "pie", secondaryPoints: breakdownPoints },
  };
  const chartPlan = reportChartPlan[selected] || reportChartPlan["Revenue report"];
  const chartFormat: "count" | "money" = selected === "Inventory movement" || selected === "Equipment report" ? "count" : "money";
  useEffect(() => {
    let cancelled = false;
    const database = getLocalDatabase();
    if (!database) return;
    (async () => {
      const [bookingsRes, expensesRes, inventoryRes, cashRes, equipmentRes, maintenanceRes, paymentsRes, movementRes, packagesRes] = await Promise.all([
        database.from("bookings").select("id, total_amount, paid_amount, status, package_id, event_date"),
        database.from("expenses").select("id, amount, status, expense_date, category"),
        database.from("inventory_items").select("id, on_hand, average_unit_cost, selling_price, name, reorder_level"),
        database.from("cash_transactions").select("id, amount, direction, status, transaction_date"),
        database.from("equipment").select("id, name, status"),
        database.from("equipment_maintenance").select("id, cost, equipment_id, scheduled_date"),
        database.from("payments").select("id, amount, payment_date, booking_id, voided_at"),
        database.from("inventory_movements").select("id, quantity, movement_type, created_at"),
        database.from("service_packages").select("id, name"),
      ]);
      if (cancelled) return;
      const queryError = [bookingsRes, expensesRes, inventoryRes, cashRes, equipmentRes, maintenanceRes, paymentsRes, movementRes, packagesRes].find((result) => result.error)?.error;
      if (queryError) { setReportError(queryError.message); return; }
      setReportError("");
      const bookings = ((bookingsRes.data || []) as unknown) as Array<{ id: string; total_amount: number; paid_amount: number; status: string; package_id: string | null; event_date: string }>;
      const expenses = ((expensesRes.data || []) as unknown) as Array<{ id: string; amount: number; status: string; expense_date: string; category: string }>;
      const inventory = ((inventoryRes.data || []) as unknown) as Array<{ id: string; on_hand: number; average_unit_cost: number; selling_price: number; name: string; reorder_level: number }>;
      const txs = ((cashRes.data || []) as unknown) as Array<{ id: string; amount: number; direction: string; status: string; transaction_date: string }>;
      const equipment = ((equipmentRes.data || []) as unknown) as Array<{ id: string; name: string; status: string }>;
      const equipmentNames = new Map(equipment.map((item) => [item.id, item.name]));
      const maintenance = ((maintenanceRes.data || []) as unknown) as Array<{ id: string; cost: number; equipment_id: string; scheduled_date: string }>;
      const payments = ((paymentsRes.data || []) as unknown) as Array<{ id: string; amount: number; payment_date: string; booking_id: string; voided_at: string | null }>;
      const movements = ((movementRes.data || []) as unknown) as Array<{ id: string; quantity: number; movement_type: string; created_at: string }>;
      const packageNames = new Map((((packagesRes.data || []) as unknown) as Array<{ id: string; name: string }>).map((item) => [item.id, item.name]));
      const bookingById = new Map(bookings.map((booking) => [booking.id, booking]));
      if (reportRange.from > reportRange.to) { setReportError("The report start date must be on or before its end date."); setReportData({ total: 0, records: 0, label: "Invalid date range" }); return; }
      const inRange = (date: string) => date >= reportRange.from && date <= reportRange.to;
      const periodBookings = bookings.filter((item) => inRange(item.event_date));
      const eventDateBookings = bookings.filter((item) => inRange(item.event_date));
      const periodPayments = payments.filter((item) => !item.voided_at && inRange(item.payment_date));
      const periodExpenses = expenses.filter((item) => inRange(item.expense_date));
      const periodTransactions = txs.filter((item) => inRange(item.transaction_date));
      const periodMovements = movements.filter((item) => inRange(item.created_at.slice(0, 10)));
      let next = { total: 0, records: 0, label: "0 records" };
      let chartRows: Array<{ date: string; value: number; group: string }> = [];
      switch (selected) {
        case "Revenue report":
          chartRows = basis === "Payment / expense date" ? periodPayments.map((p) => { const booking = bookingById.get(p.booking_id); return { date: p.payment_date, value: Number(p.amount), group: packageNames.get(booking?.package_id || "") || "Other / unassigned" }; }) : periodBookings.map((b) => ({ date: b.event_date, value: Number(b.paid_amount || 0), group: packageNames.get(b.package_id || "") || "Other / unassigned" }));
          next = basis === "Payment / expense date" ? { total: periodPayments.reduce((s, payment) => s + Number(payment.amount), 0), records: periodPayments.length, label: `${periodPayments.length} posted payments` } : { total: periodBookings.reduce((s, b) => s + Number(b.paid_amount || 0), 0), records: periodBookings.length, label: periodBookings.length + " bookings by event date" };
          break;
        case "Cashflow report":
          chartRows = periodTransactions.filter((t) => t.status === "POSTED").map((t) => ({ date: t.transaction_date, value: (t.direction === "INFLOW" ? 1 : -1) * Number(t.amount), group: t.direction === "INFLOW" ? "Cash in" : "Cash out" }));
          next = { total: periodTransactions.filter((t) => t.status === "POSTED").reduce((sum, tx) => sum + (tx.direction === "INFLOW" ? Number(tx.amount) : -Number(tx.amount)), 0), records: periodTransactions.length, label: periodTransactions.length + " transactions · net cash movement" };
          break;
        case "Outstanding balances":
          chartRows = eventDateBookings.filter((b) => Number(b.paid_amount || 0) < Number(b.total_amount || 0)).map((b) => ({ date: b.event_date, value: Math.max(0, Number(b.total_amount || 0) - Number(b.paid_amount || 0)), group: b.status }));
          next = { total: eventDateBookings.reduce((s, b) => s + Math.max(0, Number(b.total_amount || 0) - Number(b.paid_amount || 0)), 0), records: eventDateBookings.filter((b) => Number(b.paid_amount || 0) < Number(b.total_amount || 0)).length, label: eventDateBookings.filter((b) => Number(b.paid_amount || 0) < Number(b.total_amount || 0)).length + " unpaid bookings by event date" };
          break;
        case "Booking performance":
          chartRows = periodBookings.map((b) => ({ date: b.event_date, value: Number(b.total_amount || 0), group: b.status }));
          next = { total: periodBookings.reduce((s, b) => s + Number(b.total_amount || 0), 0), records: periodBookings.length, label: periodBookings.length + " bookings" };
          break;
        case "Package performance":
          chartRows = periodBookings.filter((b) => b.package_id).map((b) => ({ date: b.event_date, value: Number(b.total_amount || 0), group: packageNames.get(b.package_id || "") || "Archived package" }));
          next = { total: periodBookings.reduce((s, b) => s + Number(b.total_amount || 0), 0), records: periodBookings.filter((b) => b.package_id).length, label: periodBookings.filter((b) => b.package_id).length + " package bookings" };
          break;
        case "Expense report":
          chartRows = periodExpenses.filter((e) => e.status === "PAID").map((e) => ({ date: e.expense_date, value: -Number(e.amount), group: e.category || "Uncategorized" }));
          next = { total: periodExpenses.filter((e) => e.status === "PAID").reduce((s, e) => s + Number(e.amount), 0), records: periodExpenses.length, label: periodExpenses.length + " expenses" };
          break;
        case "Profitability report":
          chartRows = periodTransactions.filter((t) => t.status === "POSTED").map((t) => ({ date: t.transaction_date, value: (t.direction === "INFLOW" ? 1 : -1) * Number(t.amount), group: t.direction === "INFLOW" ? "Income" : "Operating costs" }));
          next = { total: periodTransactions.filter((t) => t.status === "POSTED").reduce((sum, tx) => sum + (tx.direction === "INFLOW" ? Number(tx.amount) : -Number(tx.amount)), 0), records: periodTransactions.length, label: periodTransactions.length + " posted cash movements" };
          break;
        case "Inventory valuation":
          chartRows = inventory.map((i) => ({ date: "", value: Number(i.on_hand) * Number(i.average_unit_cost), group: i.name }));
          next = { total: inventory.reduce((s, i) => s + Number(i.on_hand) * Number(i.average_unit_cost), 0), records: inventory.length, label: inventory.length + " items" };
          break;
        case "Inventory movement":
          chartRows = periodMovements.map((m) => ({ date: m.created_at.slice(0, 10), value: (m.movement_type === "STOCK_IN" || m.movement_type === "RETURN" ? 1 : m.movement_type === "USAGE" ? -1 : 0) * Number(m.quantity), group: m.movement_type }));
          next = { total: periodMovements.reduce((sum, movement) => sum + ((movement.movement_type === "STOCK_IN" || movement.movement_type === "RETURN" ? 1 : movement.movement_type === "USAGE" ? -1 : 0) * Number(movement.quantity)), 0), records: periodMovements.length, label: `${periodMovements.length} inventory movements · net units` };
          break;
        case "Equipment report":
          chartRows = equipment.map((e) => ({ date: "", value: 1, group: e.status || "Unspecified" }));
          next = { total: equipment.length, records: equipment.length, label: equipment.length + " assets" };
          break;
        case "Maintenance report":
          chartRows = maintenance.filter((m) => inRange(m.scheduled_date)).map((m) => ({ date: m.scheduled_date, value: Number(m.cost), group: equipmentNames.get(m.equipment_id) || "Unknown equipment" }));
          next = { total: chartRows.reduce((s, row) => s + row.value, 0), records: chartRows.length, label: chartRows.length + " scheduled maintenance records" };
          break;
      }
      const groupedTrend = new Map<string, number>();
      const useMonthlyBuckets = reportRange.from.slice(0, 7) !== reportRange.to.slice(0, 7) || reportRange.from.slice(0, 4) !== reportRange.to.slice(0, 4);
      chartRows.filter((row) => row.date && inRange(row.date)).forEach((row) => { const key = useMonthlyBuckets ? row.date.slice(0, 7) : row.date.slice(5); groupedTrend.set(key, (groupedTrend.get(key) || 0) + row.value); });
      const groupTotals = new Map<string, number>();
      chartRows.forEach((row) => groupTotals.set(row.group, (groupTotals.get(row.group) || 0) + (selected === "Equipment report" ? 1 : row.value)));
      setTrendPoints([...groupedTrend].sort(([a], [b]) => a.localeCompare(b)).map(([label, value]) => ({ label, value })).slice(-12));
      setBreakdownPoints([...groupTotals].filter(([, value]) => value !== 0).sort((a, b) => Math.abs(b[1]) - Math.abs(a[1])).slice(0, 8).map(([label, value]) => ({ label: label.length > 16 ? `${label.slice(0, 15)}…` : label, value })));
      setReportData(next);
      setGeneratedAt(new Date().toLocaleString("en-PH", { timeZone: "Asia/Manila" }));
    })();
    return () => { cancelled = true; };
  }, [selected, period, basis, dateFrom, dateTo]);
  const money = (value: number) => "₱" + value.toLocaleString("en-PH", { minimumFractionDigits: 2 });
  const resetFilters = () => { setPeriod("This month"); setBasis("Booking event date"); setDateFrom(`${manilaDateKey(new Date()).slice(0, 7)}-01`); setDateTo(manilaDateKey(new Date())); };
  const exportCsv = () => {
    const csv = "Report,Period,Basis,Total,Records,Label\n\"" + selected + "\",\"" + period + "\",\"" + basis + "\",\"" + reportData.total + "\",\"" + reportData.records + "\",\"" + reportData.label + "\"\n";
    const url = URL.createObjectURL(new Blob([csv], { type: "text/csv;charset=utf-8" }));
    const link = document.createElement("a");
    link.href = url;
    link.download = selected.toLowerCase().replaceAll(" ", "-") + ".csv";
    link.click();
    URL.revokeObjectURL(url);
  };
  return (
    <>
      <section className="page-heading"><div><p className="eyebrow">Insights / Reports</p><h1>Reports</h1><p className="subheading">Turn your operational records into clear business decisions.</p></div><div className="heading-actions"><button className="secondary-button" type="button" onClick={exportCsv}><Download aria-hidden="true" /> Export CSV</button><button className="primary-button" type="button" onClick={() => window.print()}><FileDown aria-hidden="true" /> Export PDF</button></div></section>
      <section className="report-toolbar"><label>Period<select value={period} onChange={(event) => setPeriod(event.target.value)}><option>This month</option><option>Last month</option><option>This quarter</option><option>This year</option><option>Custom date range</option></select></label><label>Report basis<select value={basis} onChange={(event) => setBasis(event.target.value)}><option>Booking event date</option><option>Payment / expense date</option></select></label>{period === "Custom date range" && <><label>From<input type="date" value={dateFrom} onChange={(event) => setDateFrom(event.target.value)} /></label><label>To<input type="date" value={dateTo} onChange={(event) => setDateTo(event.target.value)} /></label></>}<span className="report-cache">{reportRange.from} → {reportRange.to} · Generated {generatedAt}</span></section>
      {reportError && <p className="form-warning" role="alert">Report could not be generated: {reportError}</p>}<section className="reports-layout"><aside className="report-catalog panel"><div className="panel-heading"><div><p className="eyebrow">Catalog</p><h2>All reports</h2></div><FileBarChart aria-hidden="true" /></div><div className="report-groups"><p>Business performance</p>{reportCatalog.slice(0, 7).map((report) => <button className={selected === report ? "active" : ""} type="button" key={report} onClick={() => setSelected(report)}><span>{report}</span><ArrowRight aria-hidden="true" /></button>)}<p>Operations</p>{reportCatalog.slice(7).map((report) => <button className={selected === report ? "active" : ""} type="button" key={report} onClick={() => setSelected(report)}><span>{report}</span><ArrowRight aria-hidden="true" /></button>)}</div></aside><article className="report-detail panel"><div className="report-detail-head"><div><p className="eyebrow">{reportRange.from} – {reportRange.to} · {selected}</p><h2>{selected}</h2></div><span className="report-ready"><i /> Live database data</span></div><div className="report-kpis"><div><span>{selected === "Inventory movement" ? "Net units moved" : "Total"}</span><strong>{selected === "Equipment report" || selected === "Inventory movement" ? String(reportData.total) : money(reportData.total)}</strong></div><div><span>Records</span><strong>{reportData.records}</strong></div><div><span>Last generated</span><strong>{generatedAt}</strong></div></div><div className="report-analytics-grid"><ReportAnalyticsChart title={chartPlan.primaryTitle} kind={chartPlan.primaryKind} points={chartPlan.primaryPoints} format={chartFormat} /><ReportAnalyticsChart title={chartPlan.secondaryTitle} kind={chartPlan.secondaryKind} points={chartPlan.secondaryPoints} format={selected === "Inventory valuation" || selected === "Equipment report" ? "percent" : chartFormat} /></div><div className="report-table-placeholder"><div><p className="eyebrow">Report results</p><h3>{reportData.records === 0 ? "No matching records" : "Calculated summary"}</h3><p>{reportData.label} · {reportRange.from} to {reportRange.to}</p></div><button className="secondary-button" type="button" onClick={resetFilters}>Reset filters <ArrowRight aria-hidden="true" /></button></div><div className="scope-statement"><ShieldCheck aria-hidden="true" /><span>Figures are calculated from the connected PostgreSQL database; cashflow and expense reports use posted transactions only.</span></div></article></section>
    </>
  );
}function SettingsView({ user }: { user: AuthUser }) {
  const [databaseHealthy, setDatabaseHealthy] = useState(false);
  const [healthError, setHealthError] = useState("");
  const [status, setStatus] = useState<{ clients: number; packages: number; bookings: number; inventory: number; accounts: number; transactions: number; equipment: number; maintenance: number }>({ clients: 0, packages: 0, bookings: 0, inventory: 0, accounts: 0, transactions: 0, equipment: 0, maintenance: 0 });
  useEffect(() => {
    let cancelled = false;
    const database = getLocalDatabase();
    if (!database) return;
    fetch("/api/local-db", { cache: "no-store" }).then(async (response) => { const result = await response.json(); if (!response.ok || result.status !== "ok") throw new Error(result.error || "Local PostgreSQL is unreachable."); if (!cancelled) { setDatabaseHealthy(true); setHealthError(""); } }).catch((error: unknown) => { if (!cancelled) { setDatabaseHealthy(false); setHealthError(error instanceof Error ? error.message : "Local PostgreSQL is unreachable."); } });
    (async () => {
      const [clients, packages, bookings, inventory, accounts, transactions, equipment, maintenance] = await Promise.all([
        database.from("clients").select("id", { count: "exact", head: true }),
        database.from("service_packages").select("id", { count: "exact", head: true }),
        database.from("bookings").select("id", { count: "exact", head: true }),
        database.from("inventory_items").select("id", { count: "exact", head: true }),
        database.from("cash_accounts").select("id", { count: "exact", head: true }),
        database.from("cash_transactions").select("id", { count: "exact", head: true }),
        database.from("equipment").select("id", { count: "exact", head: true }),
        database.from("equipment_maintenance").select("id", { count: "exact", head: true }),
      ]);
      if (cancelled) return;
      setStatus({
        clients: clients.count || 0,
        packages: packages.count || 0,
        bookings: bookings.count || 0,
        inventory: inventory.count || 0,
        accounts: accounts.count || 0,
        transactions: transactions.count || 0,
        equipment: equipment.count || 0,
        maintenance: maintenance.count || 0,
      });
    })();
    return () => { cancelled = true; };
  }, []);
  return (
    <>
      <section className="page-heading"><div><p className="eyebrow">Workspace / Settings</p><h1>Settings</h1><p className="subheading">Configure connections and business rules once, then let the system stay consistent.</p></div></section>
      <section className="settings-grid">
        <article className="settings-card"><div className="settings-card-icon"><ShieldCheck aria-hidden="true" /></div><div><p className="eyebrow">Data source</p><h2>Business data</h2><p>{healthError || "Local PostgreSQL · local machine only"}</p></div><span className={"settings-status " + (databaseHealthy ? "ready" : "pending")}>{databaseHealthy ? "Healthy" : "Unreachable"}</span><button className="secondary-button" type="button" onClick={() => window.location.reload()}>Check again</button></article>
        <ProfileSettingsCard user={user} />
        <GoogleCalendarSetupSettings />
        <article className="settings-card"><div className="settings-card-icon"><Users aria-hidden="true" /></div><div><p className="eyebrow">Access</p><h2>Team accounts</h2><p>Admins can create and manage user or admin accounts.</p></div><span className="settings-status ready">{user.role}</span></article>
      </section>
      <section className="settings-summary">
        <h2>Workspace status</h2>
        <div className="settings-summary-grid">
          <div><span>Clients</span><strong>{status.clients}</strong></div>
          <div><span>Service packages</span><strong>{status.packages}</strong></div>
          <div><span>Bookings</span><strong>{status.bookings}</strong></div>
          <div><span>Inventory items</span><strong>{status.inventory}</strong></div>
          <div><span>Cash accounts</span><strong>{status.accounts}</strong></div>
          <div><span>Cash transactions</span><strong>{status.transactions}</strong></div>
          <div><span>Equipment</span><strong>{status.equipment}</strong></div>
          <div><span>Maintenance records</span><strong>{status.maintenance}</strong></div>
        </div>
      </section>
    </>
  );
}

function ProfileSettingsCard({user}:{user:AuthUser}){const[name,setName]=useState(user.fullName);const[phone,setPhone]=useState(user.phone);const[email,setEmail]=useState(user.email);const[facebook,setFacebook]=useState(user.facebookUrl);const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);const save=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setMessage("");try{const r=await fetch("/api/auth/users",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:user.id,fullName:name,phone,email,facebookUrl:facebook})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Could not update profile.");setMessage("Profile saved.")}catch(cause){setMessage(cause instanceof Error?cause.message:"Could not update profile.")}finally{setBusy(false)}};return <article className="settings-card profile-settings"><div className="settings-card-icon"><Users aria-hidden="true"/></div><div><p className="eyebrow">Your account</p><h2>Edit profile</h2><form className="profile-edit-form" onSubmit={save}><label>Full name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Phone / GCash<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone number"/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Facebook link<input type="url" value={facebook} onChange={e=>setFacebook(e.target.value)} placeholder="https://facebook.com/..."/></label><button className="secondary-button" type="submit" disabled={busy}>{busy?"Saving…":"Save profile"}</button>{message&&<small role="status">{message}</small>}</form></div></article>}

function GoogleCalendarSettings() {
  const [state, setState] = useState<{ configured: boolean; connected: boolean; email?: string; calendar?: string; failures: number }>({ configured: false, connected: false, failures: 0 });
  useEffect(() => { let cancelled = false; void fetch("/api/google-calendar", { cache: "no-store" }).then((response) => response.json()).then((result) => { if (!cancelled) setState({ configured: Boolean(result.configured), connected: Boolean(result.connected), email: result.connection?.account_email, calendar: result.connection?.calendar_name, failures: Number(result.sync?.failed_count || 0) }); }).catch(() => undefined); return () => { cancelled = true; }; }, []);
  return <article className="settings-card"><div className="settings-card-icon"><CalendarRange aria-hidden="true" /></div><div><p className="eyebrow">Integration</p><h2>Google Calendar</h2><p>{state.connected ? state.email + " · " + state.calendar + (state.failures ? " · " + state.failures + " event sync failures" : " · sync healthy") : state.configured ? "OAuth is configured; connect a Google account from the Calendar tab." : "Local OAuth client credentials are required to enable account connection."}</p></div><span className={"settings-status " + (state.connected ? state.failures ? "pending" : "ready" : "pending")}>{state.connected ? state.failures ? "Sync attention" : "Connected" : state.configured ? "Ready to connect" : "Setup required"}</span><button className="secondary-button" type="button" onClick={() => { if (state.connected) { window.localStorage.setItem("salikha-active-view", "Calendar"); window.location.reload(); } else window.location.href = "/api/google-calendar/connect"; }} disabled={!state.configured}>{state.connected ? "Manage in Calendar" : "Connect account"}</button></article>;
}

function TeamView({ currentUser }: { currentUser: AuthUser }) {
  const [users,setUsers]=useState<Array<{id:string;fullName:string;email:string;username:string;role:string;phone:string;isActive:boolean;profilePhoto:string|null}>>([]);
  const [error,setError]=useState(""); const [busy,setBusy]=useState(false); const [photo,setPhoto]=useState("");
  const [form,setForm]=useState({fullName:"",phone:"",email:"",facebookUrl:"",username:"",password:"",role:"USER"});
  const reload=()=>fetch("/api/auth/users",{cache:"no-store"}).then(r=>r.json()).then(x=>{if(!x.users)throw new Error(x.error||"Could not load accounts.");setUsers(x.users)}).catch(e=>setError(e instanceof Error?e.message:"Could not load accounts."));
  useEffect(()=>{void reload()},[]);
  const update=(key:keyof typeof form,value:string)=>setForm(old=>({...old,[key]:value}));
  const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/auth/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,profilePhoto:form.role==="USER"?photo:null})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Could not create account.");setForm({fullName:"",phone:"",email:"",facebookUrl:"",username:"",password:"",role:"USER"});setPhoto("");await reload()}catch(cause){setError(cause instanceof Error?cause.message:"Could not create account.")}finally{setBusy(false)}};
  return <><section className="page-heading"><div><p className="eyebrow">Administration / Access</p><h1>Team access</h1><p className="subheading">Create individual sign-ins. Admins manage accounts; team members use the workspace.</p></div></section><section className="settings-grid"><article className="settings-card"><div className="settings-card-icon"><ShieldCheck/></div><div><p className="eyebrow">Signed in</p><h2>{currentUser.fullName}</h2><p>{currentUser.email} · Admin</p></div></article></section><section className="panel team-panel"><div className="panel-heading"><div><p className="eyebrow">Accounts</p><h2>Create account</h2></div></div><form className="team-create-form" onSubmit={submit}><label>Account type<select value={form.role} onChange={e=>update("role",e.target.value)}><option value="USER">Team user</option><option value="ADMIN">Administrator</option></select></label>{form.role==="USER"&&<label>Profile photo<input type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>{const file=e.target.files?.[0];if(!file)return;if(file.size>1024*1024){setError("Photo must be smaller than 1 MB.");return}const reader=new FileReader();reader.onload=()=>setPhoto(String(reader.result));reader.readAsDataURL(file)}}/></label>}<label>Full name<input value={form.fullName} onChange={e=>update("fullName",e.target.value)} required/></label><label>Phone / GCash<input value={form.phone} onChange={e=>update("phone",e.target.value)}/></label><label>Email<input type="email" value={form.email} onChange={e=>update("email",e.target.value)} required/></label><label>Facebook link<input type="url" value={form.facebookUrl} onChange={e=>update("facebookUrl",e.target.value)}/></label><label>Username<input value={form.username} onChange={e=>update("username",e.target.value)} required minLength={3}/></label><label>Password (12+ characters)<input type="password" value={form.password} onChange={e=>update("password",e.target.value)} required minLength={12}/></label>{error&&<p className="form-warning wide-field" role="alert">{error}</p>}<button className="primary-button" type="submit" disabled={busy}>{busy?"Creating…":"Create account"}</button></form><div className="team-account-list">{users.map(user=><article key={user.id} className="team-account-row">{user.profilePhoto?<img src={user.profilePhoto} alt=""/>:<div className="avatar">{user.fullName.slice(0,1)}</div>}<div><strong>{user.fullName}</strong><small>{user.email} · @{user.username} · {user.phone||"No phone"}</small></div><span className="settings-status ready">{user.role}</span></article>)}</div></section></>;
}
